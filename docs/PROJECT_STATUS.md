# GroundGrid 月球车项目状态

最后核对日期：2026-09-06

最新进展：df032de 的五参数辨识与三轮独立 reachable_cost / arcs / mixed /
n_trials=3 闭环已核验通过。36/36巡回到达并完成、9/9困难目标完成安全吸附
终点、3/3无解任务正确 Abort；碰撞/陈旧均为0、原子合规率1.0。辨识最大
绝对误差0.011871≤0.02。434份归档文件散列匹配、121份快照无丢失，
包含预检目标的完整规划尝试最大耗时209.746ms。详见
[三轮弧线及辨识验证记录](validation/2026-09-06-arcs-smoke.md)。

下一步保持该代码，先显式运行一次 dynamics / mixed / n_trials=3；不重复
辨识或三轮弧线。两种基元审核后统一决定默认启用及复核完整包测试，再做
五场景各 n_trials=10。当前默认仍为旧策略，正式基线及整体验收均未完成。
困难目标完成按安全吸附终点统计，不等于到达原请求坐标。

此前 fabd8a0 完整包测试归档确认 47 项 Python 单元测试通过，但旧策略
legacy_nearest 的第三困难目标 (8,-5) 在 12.815 秒后 Abort；12/12 巡回和前
两个困难目标完成，碰撞/陈旧为 0，输出合规率 1.0。实际 XML 为 50 tests、
1 failure，包测试未通过。TEST_RC=0 来自 catkin 内置汇总错误地扫描到0项测试，
显式目录的 RESULTS_RC=1 才是本轮真实结果。详见
[包测试失败记录](validation/2026-09-06-package-test.md)。

不重复旧策略包测试；随后新策略的辨识和三轮独立闭环已完成上述验证。
完整包测试零失败仍待两种基元审核后重新核验，并未取消该验收条件。
归档验证器已补强为直接检查 XML 断言与最低测试数量，防止返回码误报成功；
新增回归连同原测试共50项在 Windows 通过，真实失败归档也被准确拒绝。

此前 fabd8a0 的 Ubuntu 同输入重放关已通过：39 份输入、468 次重放的
导出安全检查及 78 个固定预算组的重复性均通过；旧策略 234 次输出与原记录
逐值一致。新策略在第三困难目标的一份输入上将路线 8.414 m 缩至 4.645 m，
核心耗时中位数 315.111 ms 降至 24.238 ms。完整性与适用边界见
[Ubuntu 重放审核](validation/2026-09-06-ubuntu-replay.md)。
此前的输入采集及重放不需要重复；新策略闭环产生的是新的实测输入。
默认策略未改变，尚未建立正式基线。

此前已收到并校验 7a7294f 的完整旧策略 mixed/n_trials=3 采集包：
8 项预检通过，12/12 巡回及 3/3 可解困难目标完成，无解目标正确 Abort，
碰撞和陈旧跟踪为 0，原子合规率 1.0。39 份快照无丢失，153 份清单文件
SHA-256 均匹配。用户报告构建成功，但完整构建警告尚未收到。
这仅是一轮冒烟，尚未建立正式基线。

真实快照的 Windows 全量对照发现新策略在空安全目标集合上耗满 1 秒的回归，
现已加本次调用内的候选存在性探测与缓存修复；旧/新策略仍使用同一搜索队列
及原安全/预算规则。改前改后各 468 次重放均通过安全审核和固定预算重复性检查；
修复后 Windows 空集合拒绝约 1–6 ms；现 Ubuntu 同输入审核也已完成，
其 13 份空候选集输入的拒绝中位数为 0.876–3.295 ms。详情及指标边界见
[首次规划采集验证记录](validation/2026-09-06-planning-capture.md)。

接手基线：`main` / `14e0fde`；当前工作分支：`codex/p0-trajectory-fixes`（尚未合并）。此前待修复的 Ubuntu 失败基线为 `72df499` 的完整 `mixed / n_trials=3`：普通闭环、12/12 巡回和前两个可解困难目标完成，无解目标约 14 秒后正确 Abort；第三困难目标 `(8,-5)` 仍因 35 秒超时而失败，`TEST_RC=2`。本轮零碰撞、输出一致性 1.0、陈旧停车为 0。固定日志路线的生产跟踪核心离线复现支持“搜索/恢复延迟加上长倒车绕行超过预算”，不证明实时地图下最终必然完成。此后用户授权先做局部闭环正式基线，以及范围内的可达路线优先吸附；不放宽超时或安全阈值。P0 仍未验收，正式五场景基线尚未建立。

### 本批开发记录（首次 Ubuntu 采集之前）

- 从 ROS 节点提取 `LatticePlannerCore`，共享原足迹、扫掠、基元和速度曲线实现。
  首批仍为旧的首个有效距离环吸附策略；新策略尚未启用。
- `PlanningGrid` 保存地图几何、循环索引和实际缓存。Windows 已验证循环索引、
  正向/倒车/旋转、非法输入及既有安全自检；Ubuntu 必须再运行与真实 grid_map 的
  `planning_grid_parity_selfcheck`，不能把 Windows 结果视作 ROS 等价性证明。
- 新增版本化 `.ggsnap`，无损保存时间戳、NaN 位型、有效参数和内存中的基元。
  记录点在实际规划调用处；写盘独立线程，满队列和 IO 失败显式计数。
- `replay_planning_snapshot` 调用同一生产核心；固定扩展次数与真实时间预算分开。
  本地读写往返、损坏检测和生产规划结果往返测试通过；尚无真实 ROS 地图快照。
- `/lunar_planner/planning_attempt` 每次计算发布唯一 `attempt_id`，测试按任务关联去重；
  原诊断 `plan_ms` 的重复样本不再进入性能统计。`make_plan` 服务查询不改当前任务状态。
- 19 个 Python 文件语法、10 个 XML、35 项 NumPy/遥测单元测试及现有纯 C++ 自检通过。
  原六份 Claude 归档 SHA-256 不变。ROS 构建、闭环及正式基线均仍待 Ubuntu 执行。

第二批加入 `reachable_cost` 吸附策略：同一搜索队列比较范围内可达安全终点，使用
现有路线及吸附代价；不扩大范围、清障间距或时限。合成贯穿墙体测试中，旧策略
选择墙对侧候选并耗尽 300 次扩展，新策略 2 次扩展找到本侧安全候选；这不是实景
性能指标。默认仍为 `legacy_nearest`，用于首轮 Ubuntu 等价性和输入采集。
通过 `replay_planning_snapshot --strategy reachable_cost` 做同输入对照；真实闭环问题
尚未宣称解决，也尚未基于实测数据进行缓存或算法参数调优。

提交拆分：5943808 为旧策略等价提取及快照；0c94c30 为可选择的新吸附策略。
随后补上独立运行归档、实际参数/基元模式核对、五参数辨识入口、批量同输入对照、
导出折线扫掠审核和失败/中断归档测试。操作见 docs/PLANNING_REPLAY.md。
归档包含完整原输出、唯一尝试与任务总耗时，ROS 适配总耗时与离线核心时长分列。
当前没有新增 Ubuntu 结果；第一步是旧策略采集，不是宣布 (8,-5) 已修复。

本批最终 Windows 检查：22 份 Python 语法、10 份 XML、47 项纯 Python/NumPy/
观测/归档单元测试通过。7 个纯 C++ 自检均用 GCC 13.2、C++17、O2、
-Wall -Wextra -Werror 编译并运行通过；包括非均匀循环缓存、两种基元的正/倒车/
旋转、墙体候选、未知/非有限输入、预算、导出安全审核、快照往返和损坏检测。
v1 历史快照仍能读取；单份合成快照的旧/新策略各两次固定预算与计时重放通过，
固定预算输出一致。失败重复、中断、旧 JSON、缺失 XML/快照及模式回退均有
归档测试。六份 Claude SHA-256 与原清单一致。以上均不是 ROS 构建或闭环证明。

状态：可进行 ROS 仿真闭环的月球车感知与局部规划原型，尚未达到任务书整体验收或真实车辆交付状态。

## 1. 项目目标与当前边界

项目从 GroundGrid LiDAR 地形分割代码扩展而来，目标是实现月球车环境感知、风险建图、局部路径规划、速度规划、跟踪控制和异常恢复，最终面向 Atlas 200i 平台部署。

当前代码已经覆盖仿真环境中的局部闭环导航，但尚未覆盖完整的公里级任务规划、多传感器感知、长期地图记忆和目标硬件部署。因此，对外应称为“仿真闭环原型”或“局部导航原型”，不能称为验收完成版本。

## 2. 当前数据链路

```text
lunar_surface_sim.py / 真实 PointCloud2、Odometry、TF
    -> GroundGridNodelet + GroundSegmentation
    -> /groundgrid/grid_map
    -> LunarTraversabilityNode
    -> /terrain/grid_map + /terrain/costmap
    -> StateLatticePlannerNode
    -> /lunar_planner/trajectory（Path + 一一对应的 Twist）
    -> LunarPathFollowerNode
    -> /cmd_vel
```

`/lunar_planner/path` 和 `/lunar_planner/velocity_profile` 仍锁存发布，供 RViz
和旧工具兼容，但不再作为跟踪器控制输入。

## 3. 已实现能力

### 感知与地形表达

- GroundGrid 点云栅格化、地面候选判定、插值和分割。
- 高程、坡度及方向、粗糙度、台阶高度、障碍高度/置信度、观测状态和观测年龄图层。
- 60 m 滚动 `grid_map`，当前月面配置分辨率为 0.15 m。
- 真实射线式多线激光雷达仿真，而不是旧的笛卡尔均匀点阵。
- 观测空隙填充、LiDAR 近场盲区处理和随时间衰减的可通行性判断。

### 规划与控制

- 滑移转向动力学模型、逆命令补偿和运动基元库。
- 16 航向状态格、理想弧线或动力学运动基元、加权 A* 搜索。
- 坡度、风险、倒车、转向和路径长度等多项代价。
- 1.8 m × 1.5 m 矩形车体碰撞检测，以及原地旋转的扫掠检测。
- 目标吸附、吸附安全边界、路径和线角速度曲线输出。
- Relax、Rotate、BackOut、Abort 恢复阶梯及状态/诊断话题。
- 原子轨迹跟踪、规划角速度前馈与几何反馈混合，以及一次性滑移逆补偿。
- 轨迹和地形独立新鲜度门控，以及锁存的跟踪器状态话题。

### 仿真与评估

- `mixed`、`flat`、`dense`、`slope`、`negative` 五类场景。
- 闭环到达、规划成功率、避障、近障恢复、规划耗时、绕行率、跟踪 RMSE、安全距离、CPU、RSS、异常率和输出一致性统计。
- 不可达陡坡目标与可达目标分开计分；碰撞按矩形车体到真值障碍计算。

## 4. 当前风险、P0 修复状态与待验证项

### P0：重新建立可信基线

仓库中的两份 `roslog/rostest-*.log` 均为 2026-07-31 的旧失败记录：`0 errors, 1 failures, 2 tests`。Claude 历史笔记记载后续 Ubuntu 端曾修到通过，但最终五场景、每场景 `n_trials=10` 的 JSON 和 rostest XML 没有随当前提交保存。

在重新运行前，不应把历史数字当作当前提交的正式验收结果。尤其不要引用 Claude 笔记中标为“作废”的旧 LiDAR 仿真指标。

### P0 实现完成、待 Ubuntu 验证：轨迹竞态与跟踪新鲜度

规划器新增锁存的 `groundgrid/LunarTrajectory`，原子携带等长 Path/Twist；跟踪器只订阅该话题。路径/轨迹和地形新鲜度已拆分，超时均为 3.0 s，并发布 `tracking`、`goal_reached`、空/非法/陈旧轨迹、陈旧地形和 TF 不可用状态。仍需在 Ubuntu 正常闭环中证明不会出现 `stale_trajectory` 或 `stale_terrain`。

### P0 实现完成、待 Ubuntu 验证：规划速度语义和角速度闭环

理想弧线和动力学基元现在都输出滑移补偿前的期望有效车体速度。动力学基元原始轮端命令先通过 `effectiveTwist()` 转换；地形缩放和速度/加速度包络统一在规划器端执行。跟踪器直接采用规划线速度，以固定 0.5 权重混合规划角速度和几何反馈，再调用一次 `inverseCommand()`。纯 C++ 核心已覆盖正向、倒车、原地旋转、角速度前馈、限幅和非有限输入；ROS 闭环仍需验证。

### 当前 P0：短诊断通过，完整巡回仍有未完成目标

`80e5d56` 的 `mixed / diagnostic_straight=true` 在 Ubuntu 返回 `DIAG_RC=0`，但同一输出中，车到执行终点的距离曾从 0.282 m 增至 0.835 m，之后倒车返回并由规划器确认完成。因此这只能证明该次最终到达，不能证明末端跟踪已稳定；短诊断还会跳过多轮统计测试，不能把 rostest 摘要中的两项 passed 当作完整巡回通过。

代码审查和合成复现已确认旧选择逻辑存在两个结构性缺口：全路径最近点搜索可能跳过未完成旋转，前视/零速度回查可能越过前进—倒车切换边界。当前修复把目标选择及速度混合提取到 `TrajectoryTracking.h`：

- 最近点、前视和命令回查只在当前运动段内进行；段末位置与航向都取得后才允许进入下一段。
- 同几何轨迹重发保留已完成进度，真正重规划才重置；原地旋转按逐个航向样本执行，避免把长角度扫掠折成反向短转。
- 进入位置保持后的航向纠偏不再重放已结束平移弧线的角速度；已越过的旋转前馈不得反向抵消纠偏。混合权重仍为 0.5，速度上限、超时和验收距离未改变。
- `trajectory_tracking_selfcheck` 用生产跟踪核心进行连续多步回放；调试输出增加轨迹编号、全部位姿/速度及当前运动段和命令索引，供 Ubuntu 对照实际执行顺序。
- `test_closed_loop` 对普通目标也要求规划器与跟踪器双方完成，防止超时时碰巧位于 0.5 m 范围内被误报为任务完成。

这些改变已通过下述 Windows 离线检查，并收到 `878cbdf` 在 Ubuntu 上的三项短闭环通过输出。直线日志显示按前进、旋转、倒车、前进顺序执行；转弯与困难目标所提供的控制采样均未发现目标/命令索引越出当前运动段或同一轨迹的运动段进度倒退。部分控制行被其他节点输出交错截断，不能把这些摘录当作逐控制周期完整验证。感知、规划搜索、矩形/扫掠安全检查和阈值均未修改；单次诊断成功仍不代表完整巡回稳定或 P0 已验收。

后续完整巡回揭示 `reach_tour` 只是“曾进入请求目标 0.5 m 范围”的累积标记。第 10 项两端完成信号均为 False，旧 `_assert` 仍因 `reach_tour=1.0` 放行。当前已补充独立的 `completion_tour` / `completion_rate`：要求规划器与跟踪器均确认完成，并排除已记录的 timeout、aborted、shutdown；`mixed/flat` 巡回完成率仍按 99% 检查，不修改既有距离阈值或旧指标含义。新增纯 Python 计数核心与回归测试覆盖本次 11/12 的漏检模式。JSON 额外保存结束原因、试验时间戳、初始/目标航向、最终距离及规划诊断，汇总表明确区分进入距离阈值与任务完成。

`f01d8de` 在 `test/lunar_pipeline.test` 新增独立的 `debug_control` 参数，可在完整巡回期间记录控制轨迹而不跳过多轮统计。该提交仅修改测试与可观测性，未修改控制器、规划器及其参数。

### 当前修复、待 Ubuntu 验证：恢复路径交接与缓存连续性

用户补充的 `878cbdf` 完整巡回日志显示，第 10 项在固定车位连续出现 `follow_phase_invalid`：轨迹编号 150–161、164–168 都停在 `rotate_right / begin=0 / end=1`。第 13 项出现保留轨迹连接段失效、`START footprint invalid`，随后中止；第 15 项同时存在旋转阶段无效和起点车体检查失败。这些现象不能归因于“仅差几毫米”，也不能通过放宽 0.5 m 或安全阈值掩盖。

代码审查确认恢复交接缺口：Relax 生成的新目标路径已经发布，确认恢复成功后却未替换 `last_valid` 缓存，下一轮可能把恢复前的旧路径重新发布。跟踪器因此重置到旧路径首段，原地旋转锚点已在车后时会安全停车。该机制已用合成路径和生产跟踪核心复现；现有日志未包含失效锚点坐标，尚不能断言它是此次实车位姿序列的唯一原因。

- 新的纯 C++ `RetainedTrajectoryCache` 区分“可连续复用的当前路径”与“只供回退检查使用的历史路径”。确认恢复成功时，路径、速度曲线和吸附标记一起更新。
- 每次控制发布都更新缓存资格。空/非法轨迹、未确认的恢复路径、Rotate/BackOut 都撤销旧路径直接复用资格；历史数据仍保留给原有严格检查的 BackOut，不能直接重放为正常路径。
- 进展距离以当前执行的目标路径端点计算，恢复确认时从新端点重新计量，不再沿用旧缓存端点。
- `follow_phase_invalid` 增加失败原因、锚点位置/航向、锚点距离及原有到达阈值；控制律、运动段推进规则、混合权重、YAML 数值、感知与搜索算法、矩形/扫掠安全检查均未改变。

这次修复针对交接语义，不宣称解决第 13、15 项近障碍失败。`ca66499` 已收到完整三轮巡回输出，见下述目标归属问题；正式五场景验收尚未开始。

### 已收到 Ubuntu 三轮结果：跨目标状态归属与终止计数快照

`ca66499` 的第 14 项在发送目标后仅 0.0132 s 就被测试记录为中止，诊断仍是第 13 项的 `action=abort / recovery_events=2`。第 13、15 项结束原因已为 `aborted`，最后诊断却仍为 `backout`，分别停在累计中止计数 0、1；汇总仅计入 1 次中止，而日志明确记录两次 `giving up`。源码中测试在发送目标前清空状态并等待 0.5 s，这期间上一目标持续发布的无标识 `aborted` 会重新填入；状态与限频诊断分属两个话题，结束时差分计数可能少算或串入下一项。

当前修改保留所有控制参数和安全门槛：

- 规划器对每次目标生成进程内 `goal_id`，在诊断快照中原子发布请求时间戳、状态、快照序号和本目标恢复计数；完成/中止状态变化立即发布，不再等待限频诊断。
- 原子轨迹 `path.header.seq` 携带 `goal_id`，时间戳仍用于新鲜度。跟踪器新增带同一标识的 `/lunar_path_follower/diagnostics`；旧 String 状态话题保留。新目标明确重置段进度，同目标重复轨迹的进度保留规则不变。
- 测试只接收与请求时间戳及已确认目标标识匹配的快照与原子轨迹。旧的锁存状态不能终止新试验；恢复计数取终态快照中的本目标数值，不再从两个独立到达时刻做累计差分。轨迹先于确认消息到达时暂存并按标识归属。
- 保留原恢复率公式与 0.8 阈值，但明确它不是实际任务完成率；严格场景新增可解困难目标完成断言，避免仅修正漏计 Abort 就把未完成任务变成绿色。
- 连接段和起点拒绝日志增加实际 TF 位姿、实际车体检查结果、栅格位姿与地图时间戳，区分量化起点问题与实际车体已被地图判为危险；这些附加检查只用于诊断，不绕过任何拒绝。

第 13 项轨迹 `id=154` 的控制摘录显示：车辆已取得前进/旋转各段，在距执行端点 0.235 m、航向 0.328 rad 时进入原地航向校正，随后当前位姿连接段失败并撤回路径。真值最小间隙 0.470 m、无碰撞，不能据此忽略实时地图的已知危险；实际位姿与量化位姿检查哪个先失败仍需新诊断判别。第 14 项此前不能认定已被完整尝试，第 15 项同时存在接续起点困难和搜索预算耗尽。本批未宣称修复上述导航失败。

本批 Windows 检查：新增 15 项目标归属测试通过，其中 5 项直接执行流水线测试的实际回调/断言方法（使用消息形状的替身，不导入 ROS）；既有 7 项完成计数及 5 项 NumPy 辨识测试通过。两组纯 C++ 自检合计 70 项通过，18 份 Python 语法检查、10 份 XML 解析与六份 Claude 归档 SHA-256 核对通过。Windows 无 ROS，节点编译及跨进程消息时序验证仍需 Ubuntu 执行。

### 已实现、Ubuntu 完整冒烟未通过：近障起点无后继与扫掠检查一致性

`91cd94f` 的完整 `mixed / n_trials=3` 返回 `TEST_RC=2`：普通闭环通过，多轮统计因第 14、15 项未完成而失败。12 个巡回目标与第 13 项均由规划器/跟踪器确认完成；第 13 项终点为 `(-2.051,-2.512,0.323)`，距原始危险目标 0.993 m，是安全吸附完成，不是进入原请求 0.5 m 范围。所有目标均已确认，计数为恢复进入 6、确认 3、中止 3；条件恢复率 `3/(6-3)=1.0` 不代表全部任务成功。零碰撞、无陈旧跟踪、输出一致性 1.0 仅适用于此次三轮冒烟，不是五场景正式基线。

- 第 14 项目标 `(0.9,-1.6)` 在 Relax 中吸附 1.62 m，两次确认新路线后分别被新的扫掠检查撤回；行驶 2.522 m 后在 `(-1.991,-2.899,-0.319)` 中止。后续搜索只展开 1 个节点，约 4–5 ms 结束。
- 第 15 项目标 `(8,-5)` 从上述停止位姿出发，吸附 2.12 m 后同样只展开 1 个节点；行驶 0 m，9.013 s 后中止。不是 1 s 搜索预算耗尽。第 16 项陡坡目标中止符合既定预期。
- 用户保存目录为 `~/p0-results-91cd94f`，含 `full-mixed-n3.log` 和 `planner_metrics_mixed.json`；rostest 日志为 `~/.ros/log/rostest-bjm-1301000.log`。本地只收到 JSON 记录和日志摘录，未收到对应时刻的完整栅格地图。

源码确认起点只检查车体，而首个弧线/基元采样立即要求完整 0.50 m 带；保留路径却已经允许第一段恢复余量。新的纯 C++ 几何回归以日志末端位置、量化航向和合成圆形岩石构造矩形车体场景，复现旧规则 0 条可用起步边，修复后 4 条（包括端点完整余量检查）。这证明该机制有缺陷，不等于回放了当时的感知地图，也不能据此保证两个困难目标现已完成。

本次实现：

- 仅搜索根节点缺少安全带时，第一条边逐步恢复余量，段末达到完整标准，后续边不重复放宽；起点已有完整余量时不启用例外。所有物理车体、已知危险和坡度检查保留。实际 TF 到量化起点的连接段也必须安全。
- 提取生产共用 `SweptFootprint.h`，对插值位置、航向及安全带增长采样。搜索同时检查理想弧线与最终量化发布线段；动力学基元逐段扫掠并检查量化连接；BackOut 补齐历史点之间的扫掠，Rotate/保留路径复用相同检查。
- 搜索失败诊断增加目标编号、起点有效后继数、安全带检查和是否预算耗尽，取消旧日志“起点车体有效就不是起点问题”的错误推断。
- 未改变安全距离数值、吸附范围、速度、到达阈值、超时、恢复阶梯或测试断言。检查更密会增加搜索成本，Ubuntu 需同时复核规划耗时、目标完成和碰撞。

Windows 检查：新增 `planner_safety_selfcheck` 的 27 项几何检查通过，包括首段安全退出、禁止后续重开例外、原地扫掠、量化端点、倒车连接、动力学采样首段、已知/未知危险及非有限输入。另有 33 项滑移/控制自检、37 项跟踪自检及 27 项 Python 测试通过；18 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256 和 `git diff --check` 通过。ROS 节点编译和完整巡回结果仍待回传，不能将离线检查当作端到端通过。

后续 `172188e` Ubuntu 完整三轮回传：`test_closed_loop` 通过，`test_metrics_over_trials` 因三个可解困难目标 `(-1.2,-2)`、`(0.9,-1.6)`、`(8,-5)` 均中止而失败，`TEST_RC=2`。已补齐 JSON 明细与控制摘录：巡回完成 12/12，目标均确认，碰撞 0，输出一致性 1.0，陈旧跟踪 0；后三者有本轮 JSON 支持，但失败断言后面的检查没有执行完毕。条件恢复率 `1/(5-4)=1.0` 不表示困难任务完成；规划器 CPU 超预算采样比例为 0.0170，不是整机 CPU 占用。

- 第 13 项 `goal_id=14`：行驶 11.519 m，最小真值净空 0.350 m，未碰撞；实际 `(-2.029,-2.137,0.350)` 已被新地图判 `actual_body_valid=false`。随后 `start_footprint`，最终在 `(-2.430,-2.354,0.962)` 中止。不能再解释为“只有量化位姿或额外带不满足”；真值净空也不能证明应绕过感知地图。
- 第 14 项 `goal_id=15`：行驶 1.692 m，最小净空 0.525 m，最终 `(-1.896,-1.231,1.259)` 中止。时间 1788428079.729 检出原轨迹扫掠无效，接着搜索约 1 s；新轨迹起点 `(-1.975,-1.725,1.178)` 已落后于实际位置，1788428080.739 再次报实际车体无效。源码确认原流程在阻塞搜索结束后才替换/清空旧轨迹，期间跟踪器仍可移动。这是确定存在的时序缺陷，但不等于已确定全部近障失败原因。
- 第 15 项 `goal_id=16`：从上一项终点出发，0 m，`start_no_successor`；搜索只展开 1 个节点、0 后继，约 6 ms，`budget_exhausted=false`，不是规划预算不足。第 16 项陡坡中止符合既定预期。
- 保存目录为 `~/p0-results-172188e/`，含 `planner_metrics_mixed.json`、`full-mixed-n3.log` 和 `roslog/rostest-bjm-1475103.log`。本地只收到明细及摘录，未获得对应时刻完整地图；缺少具体拒绝栅格及地形层，尚不能断言为感知误报。

### 已实现、Ubuntu 停车确认已观测：无效执行轨迹先撤回，再搜索

- 复用现有车体/连接段/剩余扫掠验证，在新搜索前检查当前已发布轨迹，包括未确认的恢复路线及动作。发现无效立即发布空原子轨迹、撤销缓存复用资格并结束当前回调；历史仅保留给重新安全检查后的 BackOut。
- 新增无 ROS `ReplanStopBarrier`：等待跟踪器清空轨迹并发布零速度后，回传精确 `goal_id + trajectory_stamp_ns` 的 `empty_trajectory`，再要求一个晚于确认接收时刻的 TF 才搜索。没有确认则不搜索，新目标不取消未完成确认；防止大小为 1 的消息队列被快速新轨迹覆盖停车消息。跟踪器即使状态同为 empty，时间戳改变也重新发布确认。
- 这是软件停车命令确认，不是实际制动距离/静止反馈闭环。两个节点须一起编译部署；旧跟踪器无新时间戳字段时会保持禁止搜索，需要修正版本或通信，而非跳过安全门。
- 增加 `footprint_reject` 精确采样点、栅格位置、原因、地图时间与原始地形层，区分车体、起点安全带和具体障碍/台阶/坡度拒绝。诊断不改变原有有效性判定，不依据真值净空放行感知危险。
- 未修改速度、控制混合权重、安全余量、吸附距离、到达容差、超时、恢复阶梯或验收断言。额外执行轨迹检查的成本与停后重规划效果需在 Ubuntu 实测。

Windows 本轮检查：33 项滑移/控制、50 项跟踪/停车确认、27 项扫掠自检和 28 项 Python 测试通过；新测试覆盖精确目标/纳秒戳、重复/畸形/旧确认、确认后新 TF、同目标第二次停车、新任务不能取消旧停车，以及等待期无速度输出。Python 观测测试确认停车不是到达或陈旧。18 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256 和 `git diff --check` 通过。纯自检不覆盖 ROS 传输或物理制动；ROS 节点编译和连续完整 `mixed / n_trials=3` 尚待用户验证，不能宣称困难目标已修复。

后续 `7ff5332` Ubuntu 回传：`test_closed_loop` 通过，完整三轮仅多目标统计失败，`TEST_RC=2`。困难目标未完成列表为 `(0.9,-1.6): aborted` 和 `(8,-5): timeout`。随后收到完整困难项 JSON：巡回完成 12/12，总完成 13/16，所有目标已确认，零碰撞，输出一致性 1.0，无陈旧跟踪，所有 15 个可解目标均曾规划成功；这不等于它们全部到达。条件恢复率 0.9，规划器 CPU 超预算采样比例 0.0746（不是整机占用），尚无正式五场景基线。

- 摘录中 12 次撤回（`goal_id=7/10/11/15/16`）均匹配且各仅匹配一次相同目标及纳秒戳的停车确认，没有出现等待确认卡住的证据。这验证了 ROS 中软件确认链路确实运行，不是物理制动或整个闭环验收通过。
- 第 13 项 `goal_id=14`：11.033 s 完成，行驶 9.337 m、最小净空 0.685 m，没有恢复；规划器与跟踪器共同确认吸附完成。距原目标 0.977 m、`reached=false` 符合“吸附完成”与“进入原目标 0.5 m”分开统计的语义。
- 第 14 项 `goal_id=15`：行驶 7.443 m、最小净空 0.685 m，27.022 s 后在 `(1.046,-3.958,0.686)` 中止，恢复进入 4、确认 3、中止 1，最后 `search_timeout`。起步阶段在 `(-1.975,-3.075,-0.785)` 的 0.50 m 带内，栅格 `(-0.925,-2.475)` 报横坡约 19.04°，超过 15°；后续路径远端被撤回，吸附目标从 `(1.475,-3.075)` 换到 `(-0.625,-0.075)`，此时多次约 1 s 预算耗尽。不是单一零后继，也不能用增加试验等待时间解决已中止目标。
- 第 15 项 `goal_id=16`：行驶 7.667 m、最小净空 0.701 m，35 s 截止时在 `(6.843,-7.080,-0.194)`，恢复进入 7、确认 6、尚未中止，最后 `start_no_successor`。同一任务已六次确认又撤回远端路线；最终 0.50 m 带内格 `(7.175,-5.925)` 坡度约 21.96–22.45°，超过 20°，即使障碍置信度为零仍被坡度规则拒绝。吸附目标从西南侧 `(6.425,-6.525)` 跳到东南侧 `(9.425,-6.525)`；不能仅延长 35 s 就宣称问题解决。
- 用户保存目录为 `~/p0-results-7ff5332/`，rostest 日志 `roslog/rostest-bjm-1599188.log`。本地有测试摘要、JSON 明细和过滤日志，没有原始地图或完整构建/自检输出。部分路线确认几毫秒后就被扫掠检查撤回，但缺少两次所用地图时间戳，不能证明实际日志都是同图失效。终端额外的 `65;1;9c` 命令错误发生在测试结束后，不是 rostest 失败原因。

### 当前修复、待 Ubuntu 验证：旋转矩形的栅格覆盖单调性

源码和独立合成试验确认：旧 `footprintValid` 在车体坐标中每隔 0.15 m 撒点，再查询地图格子。旋转后存在漏格，且安全带改变会移动整组采样点。以 `(6.425,-6.525,-π/4)` 为位姿、0.15 m 栅格中的 `(5.175,-6.075)` 为单一危险格，物理车体与 0.50 m 带旧检查通过，0.25 m 带旧检查反而失败。这个格子不在物理车体内，但与两种安全带均相交。该试验证明检查缺陷，并非当时完整感知地图的回放，也不是所有剩余失败的唯一归因。

- 新增生产共用 `FootprintRaster.h`，按矩形与地图方格的四轴相交判定枚举每个触及格子一次，包括边/角接触、负坐标、子栅格偏移和滚动地图原点。较大安全带的格子集合包含较小安全带，不以外接圆代替车体。
- 搜索、恢复和执行检查继续经同一 `footprintValid` 使用新枚举；物理车体与外带的未知策略、已知危险、纵/横坡门槛、0.25/0.50 m 余量保持原值。地形平均代价改为按实际触及的唯一格子计算，可能改变选路；这不是参数调优。旧采样漏检的路线可能更早被拒绝，需复核计算开销和闭环完成情况。
- 执行扫掠拒绝增加 `context=active_sweep` 的具体格子、实际失败余量与原始地形层，且记录 `validated_map/current_map`，用来区分同图覆盖不一致与地图更新后的失效；不再只在起点无后继时才看得到格子原因。
- 未延长搜索/试验超时、未调整恢复阶梯、未改变吸附算法或到达/成功断言；两个未完成目标仍是验收缺口。

Windows 回归：38 项规划安全自检（含 11 项新增覆盖测试）通过；195 组航向/子格偏移下，三种余量与独立的多边形裁剪算法逐格比较，共 1,184,625 次均一致。另有 33 项滑移/控制、50 项跟踪/停车确认和 28 项 Python 测试通过；18 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256 和 `git diff --check` 通过。尚未在本机编译 ROS 节点或运行真实地图闭环，需 Ubuntu 完整三轮复测后再评估。

### 最新 Ubuntu 回传：`0b314f1` 的困难目标失败链

完整 `mixed / n_trials=3` 的普通闭环 passed，多轮统计首先在 `plan_success_all=13/15=0.8667 < 0.90` 失败；后面的严格困难目标完成断言尚未执行。JSON 中 12/12 巡回完成、总计 12/16 完成，三个可解困难目标均中止。恢复进入 7、确认 3、中止 4，条件恢复率 `3/(7-4)=1.0` 不能代替任务完成。CPU 超预算采样率 0.005263 仅是规划器耗时样本比例，不是全系统 CPU 占用。

- 第 13 项从 `(-10.104,-6.313,1.742)` 出发，行驶 13.537 m 后停在 `(-1.659,-1.656,-0.535)`，最小真值间隙 0.171 m，无碰撞。轨迹 183 先因最近点距离 0.979 m 撤回；轨迹 186 的前进段随后越过跟踪点仍输出 `v=0.533,w=-0.400`。
- 轨迹 186 的 `11 -> 12` 段在新地图上因 20.177° 的坡度危险格撤回；`validated_map=1788442195.696176`，`current_map=1788442196.325290`。这是地图更新后的失效，不能再归为同图大小安全带漏格。六次撤回均收到匹配的停车确认。
- 随后实际车体下 `(-1.525,-2.625)` 格子的感知坡度稳定在约 26.02°、代价 100；第 14、15 项均因 `start_footprint` 无路径、行驶 0 m，是接续失败。共享解析地形在该点的梯度模对应约 0.827°（该局部不在岩石锥体内），提示地形重建/注册需要核查；尚无该时刻原始高程邻域，不能断言全部错误都由感知造成，亦不能放宽安全门槛。
- 证据目录 `~/p0-results-0b314f1/`：`full-mixed-n3.log`、`planner_metrics_mixed.json`、`roslog/rostest-bjm-1640612.log`。本地收到 JSON 统计/明细和轨迹日志摘录，完整地图与 ROS 归档仍在 Ubuntu。

### 当前修复、待 Ubuntu 验证：点云几何与采样时间戳一致性

源码中 `publish_cloud` 在开始时锁定车位，耗时射线计算结束后却读取最新 `self.last` 作为点云时间；同时 `update` 在取得状态锁前已更新 `self.last`。二者都可能让几何对应旧位姿、Header 对应新位姿。GroundGrid 明确使用 Header 时间查询 TF，因此会错误注册运动中的点云。这是仿真输入时序缺陷，不是感知算法调参。

- 仿真状态更新现在在同一锁内提交位姿与时间；点云在同一锁内捕获位姿和时间，并一直使用捕获时间。射线计算期间不占用动力学锁，地形形状、噪声、点格式、频率、积分公式和速度限幅未改变。
- 新增 `test_lunar_sim_timing.py`，提取并运行实际仿真类的生产方法，以 ROS 形状替身确定性插入动力学更新，不依赖 ROS 或真实线程调度。旧代码在 0.4 s 的合成计算窗口中错误使用新时间，平面注册高度误差达 0.05069 m；三项回归失败。修复后五项测试通过，包括时间/位姿锁、运动点云注册、静止一致性和无回波处理。
- 该复现证明时序错误存在，不证明它已解释 `0b314f1` 的所有坡度异常。Ubuntu 构建、地图稳定性及完整困难目标仍需复测，不能用单元测试替代。

### 当前修复、待 Ubuntu 验证：角速度混合后的统一限幅

`0b314f1` 轨迹 186 的日志派生回放（使用四舍五入位姿与速度、无 ROS/地图重规划）复现前进段越过目标后继续驶离：`planned_w=0` 时，几何反馈先被限到 `-0.8`，再按固定 0.5 混合，只剩 `-0.4 rad/s` 的纠偏。前进速度仍为 0.533 m/s。这违背原 P0 计划“50% 前馈 + 50% 几何反馈，随后统一限幅”的顺序，不是必须增大角速度上限的问题。

- 平移和姿态保持现在都把未限幅的有限几何反馈交给 `blendTrajectoryCommand`，再对混合结果限幅；仍采用规划线速度、0.5 权重、原 1.5 航向反馈系数、0.8 rad/s 上限及一次 `inverseCommand`。反馈计算溢出明确拒绝；不靠限幅把非有限计算伪装成有效输入。
- 日志派生回放的第一个倒车→前进边界现在能在原 0.20 m / 10° 门槛内取得，并能使用完整 0.8 rad/s 纠偏；旧版本该段持续驶离。完整复合轨迹的后续倒车段仍出现偏差，不能把这项局部回归当作全部轨迹收敛或碰撞安全证明。
- 原始 `feedback_w` 调试值可以超过 0.8，`desired_w/cmd_w` 仍必须满足各自限幅；不应把反馈需求量误当作输出超限。
- 本次未改变目标/速度索引、段推进、感知与搜索算法、安全余量、超时或测试阈值。需要在修正点云时序后重新检查完整三轮的地图与控制耦合，困难目标仍是明确验收缺口。

Windows 验证：37 项滑移/控制检查、53 项跟踪/停车确认检查、38 项规划安全检查全部通过（本次新增 7 项限幅/局部轨迹回归）；33 项 Python 测试、19 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256 与 `git diff --check` 通过。本机未编译 ROS 节点，未运行 ROS 完整闭环。

### 最新完整明细：`b0b5d4f` 剩余两项困难目标中止

用户按完整三轮命令回传 `TEST_RC=2`，`test_closed_loop` passed，`test_metrics_over_trials` 在第 853 行列出 `(0.9,-1.6)` 和 `(8,-5)` 的 `aborted`。当前断言顺序表明零碰撞、巡回规划/进入距离/完成门槛已通过，第一困难目标不在未完成名单；这不代表多次运行稳定性。规划耗时、条件恢复率、输出一致性、陈旧跟踪等后续断言尚未执行，不能声称全部通过。

后续 JSON 摘录确认 12/12 巡回完成、总完成 13/16，名义规划成功 14/15；碰撞 0、轨迹输出一致性 1.0、陈旧跟踪 0、角速度曲线覆盖率 1.0。恢复进入 4、确认 1、中止 3，条件恢复率 `1/(4-3)=1.0` 不表示两项中止任务成功。CPU 超预算采样率 0.012048，不是全系统 CPU 占用。

- 第 13 项无恢复直接完成，终点 `(-2.07135,-2.04540,0.17025)`，行驶 10.629 m、最小真值间隙 0.616 m；原始危险目标误差 0.873 m，属于安全吸附完成，不是普通 0.5 m 到达。
- 第 14 项从该终点出发，行驶 3.451 m；Relax 将目标吸附到 `(-0.625,-0.075,0.393)` 附近并确认新路线。`1788448901.0055` 的执行扫掠 `17 -> 18` 在新地图上因横坡否决：余量 0.25 m、采样位姿 `(-0.737,-0.112,0.393)`、格子 `(-0.325,-1.125)`，坡度模 15.983°。停车确认正常，随后停在 `(-1.19264,-0.48235,0.89873)`，以 `start_no_successor` 中止；最小真值间隙 0.450 m，无碰撞。
- 第 15 项从同一停车位出发、行驶 0 m、没有名义路径，同样 `start_no_successor`，约 6.5 ms 的规划记录不支持“耗尽 1 s 预算”。起点诊断记录的是 0.50 m 额外安全带中的坡度格，不是上一轮 `actual_body_valid=false` 的证据。仅凭起点全带失败，还不能知道每条渐增余量起步边为何都失败。
- 用户证据位于 `~/p0-results-b0b5d4f/`：`planner_metrics_mixed.json`、`full-mixed-n3.log`、`roslog/rostest-bjm-1726232.log`。本地收到统计、三项明细和拒绝日志，不是完整地图；本轮 BUILD_RC/SELF_RC 尚未单独回传。

### 当前诊断增强：记录每个候选起步动作的实际拒绝采样

本批不修改搜索、感知、控制、安全余量、超时或断言；测试仍可能失败，目的是区分起步渐增余量、实际车体危险与恢复路径无法退出等原因。

- ROS-free 扫掠核心新增可选拒绝样本输出，记录实际失败位姿、当时的余量和未知例外。用于判断起点是否已有全带的探测失败不算动作失败；成功或输入无效时清空旧记录。普通调用不请求记录，判定和代价不变。
- `~debug_start_rejections` 默认关闭；完整 rostest 的 `debug_control:=true` 同时开启。仅在理想弧线模式的根节点无后继时，用同一锁内地图和生产 `transition` 复查八个动作，每个动作记录自己的失败点与具体格子，批次限频 3 s。复查不会插入开放集、选路或发布命令；动力学基元模式仍保留原诊断，不声称覆盖逐基元根动作。
- Rotate/BackOut 额外记录实际扫掠失败，或明确说明历史为空、没有更早位姿，避免把上一次搜索的 `last_fail` 当成所有恢复动作的具体拒绝原因。
- 输出有诊断开销，不能把本批 debug 运行作为正式性能基线。先复核新增纯 C++ 可观测性回归，再由 Ubuntu 构建并采集完整三轮记录；尚未证明困难目标已解决。

Windows 检查：45 项规划安全自检通过（新增 7 项拒绝记录、清理和判定/代价等价性检查），既有 37 项滑移/控制、53 项跟踪/停车确认及 33 项 Python 测试通过；19 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256、`git diff --check` 通过。ROS 节点编译与实际诊断输出待 Ubuntu 验证。

### 最新诊断结果：`eee15b7` 近障恢复被额外安全带阻断

用户回传完整 `mixed / n_trials=3 / debug_control=true` 的摘要和新增拒绝日志：`TEST_RC=2`，普通闭环通过，多目标统计仍因 `(0.9,-1.6)`、`(8,-5)` 中止而失败。当前断言顺序支持本次巡回完成门槛及第一困难目标完成；后续性能/输出/新鲜度断言未执行完毕，未收到本轮 JSON，不能沿用上一提交的具体统计。文件位于 `~/p0-results-eee15b7/full-mixed-n3.log`，原始日志为 `roslog/rostest-bjm-1765052.log`；BUILD_RC 和纯自检输出未单独回传。

- 第二困难目标 `goal_id=15` 的活动路径在 `1788451475.9193` 被新地图的横坡检查拒绝：扫掠位姿 `(0.875,-3.825,0.785)`、额外余量 0.25 m、格子 `(0.425,-2.775)`，原始坡度模 15.839°。随后根状态 `(0.725,-4.125,0.392699)` 的八种起步动作全部失败；实际拒绝余量为 0.267–0.467 m，原因是额外安全带中的横坡或 lethal cost。拒绝日志按生产逻辑先检查物理车体，正余量说明这些失败样本的车体检查通过，但不证明尚未检查的后续动作安全。
- Rotate 在实际位姿 `(0.756965,-4.091397)` 的转动途中、额外余量 0.192308 m 时被横坡拒绝。BackOut 的第一段在 `(0.620492,-4.116599,0.422724)`、余量 0.375 m 时被横坡拒绝；相对实际起点仅移动约 0.139 m。源码要求首个历史采样端点恢复完整 0.50 m，不能把配置的 1 m 回退距离误当作当前允许恢复余量的距离。
- 第三困难目标 `goal_id=16` 使用相同根状态，八种起步动作仍全部失败；Rotate 同样被额外带横坡拒绝。BackOut 明确输出 `reason=no_history`：新目标清空 `retained_route_`，不等于该车此前没有实际行驶历史，也不应直接取消清理而重新执行上一目标的缓存路径。
- 这次证据已把当前阻塞缩小到“地图更新后的额外安全带不足、第一短边无法恢复全带，以及跨目标没有独立回退历史”。它不是增加搜索时间或放宽到达阈值可以解决的现象；没有完整地图，不能证明一定存在另一条安全逃逸路线，更不能断言坡度是假障碍。

本轮只记录事实，不修改安全策略或验收断言。候选下一步是有界的近障回退恢复：将真实行驶历史与旧任务执行缓存分离，在有限回退机动内恢复额外余量，同时全程重新检查矩形车体和扫掠、禁止车体跨越已知危险，末端达到完整余量后才恢复正常规划。允许余量不足持续超过第一条短边会改变当前明确的安全策略，实施前须取得用户对此范围调整的确认；不应默默延长例外或降低余量数值。

### 经用户确认的范围调整：实际历史与有界近障回退

用户在获知 `eee15b7` 诊断、安全策略边界及公里级分层路线后明确同意实施。本次只调整 BackOut，普通搜索和 Rotate 仍按首条边恢复完整余量，感知、目标吸附、规划搜索、跟踪控制律和验收门槛不变。

- 新增无 ROS `RecentMotionHistory`，只记录有时间戳的实际定位，不把规划路径当成已行驶路线。跨目标保留观测，旧任务路径缓存与执行许可仍清空；新目标中断活动回退时先对旧 goal_id 做精确停车握手。历史限制为回退预算 4 倍角点运动量、60 s、4096 条观测；0.5 s 数据间隙、错误坐标系、非法数据或运动跳变断开历史。保留静止观测的时间戳序列，避免 TF 比最新 odometry 晚一帧时误认为历史过期。
- `BoundedBackout` 沿实际历史逆序截取，默认预算仍为 1 m，平移和角点转动均消耗预算。额外安全带按累计运动量在整个机动恢复，摆脱“首个约 0.1 m 历史片段就必须达到 0.50 m”的离散化依赖；末端需满足原完整余量。没有合格退出路径就拒绝，不延长预算寻找方便终点，不忽略车体下已知危险。
- 生成及速度曲线加密后的导出使用同一冻结余量安排并检查整段。`17450d5` 后续证据表明执行期重查已驶过前缀会被车后地图更新误撤回；当前执行复检改为实际新运动、当前连接和未执行后缀，进度仍不可回退/重开。实际采样累计运动超预算、当前/未来危险、定位断裂或跟踪偏离都会撤回。完成要求实际位姿满足原终点门槛且实际完整余量有效，不能把计划端点安全当作实际车已脱困。
- 回退执行不再每轮重新生成，也不会被普通 2 s 恢复阶梯提前切走；保持原无进展门槛，并用速度曲线估计时间加无进展门槛设置固定截止时间。完成后经停车确认重新尝试目标路径，尚不计任务成功/恢复确认；失败后经停车确认进入 Abort。以上是采样软件门控，不宣称真实制动距离或连续实际运动的硬界限。
- 新增 `backout_recovery_selfcheck` 并加入构建、安装、包测试；启动文件把现有 odometry 参数同步传给规划器。现有原子轨迹消息格式、0.5 角速度混合、速度/坡度/到达门槛和三个已有超时均未调参；数据连续性和回退执行期限是本策略明确新增的约束。

Windows 检查：新增 47 项回退核心检查通过，包含同一地图旧短段失败/整段恢复通过、跨目标实际历史、定位连续性/过期/TF 落后一帧、加密不改变余量进度、危险与未知拒绝、旋转扫掠、固定期限、运动预算，以及生产跟踪器反复重发后的倒车完成和精确停车门控。既有 37 项滑移/控制、53 项跟踪/停车、45 项规划安全检查通过，合计 182 项 C++ 检查；33 项 Python 测试、19 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256 与差异空白检查通过。尚无本次 ROS 节点编译和闭环输出；合成场景不等于 `eee15b7` 的原始感知地图回放，不保证两个困难目标已经完成。公里级全局规划和持久化风险地图仍未实现。

### 最新 Ubuntu 回传与当前修复：BackOut 门槛归属及已驶过前缀

`17450d5b31475fa6183f2f5ad32e822729ce6210` 的完整 `mixed / n_trials=3 / debug_control=true` 回传中，`test_closed_loop` passed，统计测试仍列出 `(0.9,-1.6)` 与 `(8,-5)` 中止，`TEST_RC=2`。本轮未回传单独的 `BUILD_RC`、自检摘要或 JSON，因此不能沿用其他提交的完整率值，也不能声称 ROS 构建/包测试通过。证据位于 Ubuntu 的 `~/p0-results-17450d5/full-mixed-n3.log` 和 `roslog/rostest-bjm-1813608.log`。

- 两个失败目标都在生成 BackOut 时命中同一已知横坡带：回退样本 `(0.304491,-4.292060,0.308812)`、累计进度约 85%、要求余量 0.425614 m，危险格 `(0.425,-2.925)` 的坡度约 19.25–19.64°。正余量拒绝表示生产检查先通过了该样本车体，再在额外带内拒绝；这不是允许忽略的车体危险。
- 随后的 `goal_id=17` 使用普通 0.25 m 余量，真实历史 BackOut 成功启动，记录 1409 个观测，进度从 0.069 增至 0.285，实际运动 0.361 m 后停止。这证明跨目标实际定位历史、倒车轨迹、跟踪器和停车确认链路已经运行；它不等于困难目标完成。
- 源码确认 BackOut 调用了 `trajectoryClearance()`，而该函数会读取最后一次失败 `planImpl` 留下的 `snapped_goal_used_`。因此恢复动作虽不驶向吸附终点，却错误继承 0.50 m `goal_snap_clearance`。当前改为只恢复普通 `trajectory_clearance=0.25 m`；若之后仍需吸附目标，正常搜索必须独立恢复并保持 0.50 m，吸附安全标准没有降低。
- `goal_id=17` 的活动回退在新地图上因样本 `(0.816956,-4.057612,0.553118)` 被撤回；当时车已沿冻结路线前进约 0.36 m，该拒绝样本位于已驶过前缀。旧实现每周期重查整条历史，等于让车后新发现的危险追溯性撤销已经发生的运动。当前生成阶段仍检查完整路线；执行阶段改为检查上一实际位姿到当前位姿的扫掠、当前到冻结路线的连接，以及全部尚未执行后缀。实际段或未来段上的新危险仍立即失败，累计实际运动、跟踪偏差、固定期限和终点完整余量门槛保持不变。

本地新增回归分别覆盖“仅车后出现新危险继续”“两个定位周期之间出现危险停止”和“未来后缀出现危险停止”。回退核心现为 53 项，连同 37 项滑移/控制、53 项跟踪/停车和 45 项规划安全检查，共 188 项 C++ 检查全部通过；33 项纯 Python 测试、19 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256 与差异空白检查通过。Windows 不能编译 ROS 节点或证明闭环；当前结果仍不能保证两个困难目标已解决。

### 最新 Ubuntu 回传与当前修复：自车盲区高程递推污染

`7f607247187c38b4334eb5af03004787c861d757` 的完整 `mixed / n_trials=3 / debug_control=true` 回传中，`test_closed_loop` passed，多目标统计因 `(0.9,-1.6)` 与 `(8,-5)` 中止而失败，`TEST_RC=2`。12/12 巡回及第一个可解困难目标完成，总完成 13/16；碰撞 0、输出一致性 1.0、陈旧跟踪 0。第二困难目标行驶 2.211 m 后停在 `(-1.206,-3.246,-0.973)`，后两项目从该位姿无法启动。

- 失败不再发生在额外安全带：BackOut 在 `margin=0`、`allow_unknown=true` 时即拒绝当前物理车体，`actual_body_valid=false`。危险格 `(-1.075,-2.625)` / `(-1.075,-2.775)` 的感知坡度从约 15.76° 增长到 22.24°，而障碍高度约 0.006 m、置信度 0；停车期间仍继续增长，符合逐帧插值扩散而非静态地形。
- 共享解析 `mixed` 地形在同一两格的中心差分坡度分别为 0.731° 和 0.796°，车体中心约 1.015°；格子距 `(0,-2)` 岩石轮廓仍约 0.59–0.69 m。该仿真证据确认是假坡，但不能据此在规划器绕过任何已知危险或放宽 15°/20° 门槛。
- 根因是 GroundGrid 对 `min_point_distance=1.2 m` 自车排除区每帧继续做空间插值，却只钉住单个中心格；附近岩石的高程估计可向没有回波的车体下方递推。当前修复在该排除圆盘内优先保留直接历史高程；从未获得直接高程且本帧无可用点的格子使用点云捕获时刻的车体 xy 支撑平面。圆盘外、当前有可用点的格子及历史测得的岩石/台阶仍走原安全语义。
- `elevation_raw` 不再把仅计入 `pointsRaw`、但被自车掩码拒绝的回波写成 `minGroundHeight` 哨兵值。拒绝日志增加 ground、corrected/raw elevation、观测年龄、置信度及原始/可用点数，供 Ubuntu 判断修复后是否仍有别的地图污染。
- 新增无 ROS `BlindZoneSupportPlane` 与 `blind_zone_ground_selfcheck`；覆盖平面/倾斜车体投影、直接历史证据优先、边界、当前测量优先及非法输入失败关闭。此修复尚未经过 ROS 编译和闭环，不得先记为困难目标通过。

本轮 Windows 检查：原有 188 项纯 C++ 检查与新增 11 项盲区支撑平面检查共 199 项通过；33 项纯 Python 测试、19 份 Python 语法、10 份 XML、六份 Claude 归档 SHA-256 和差异空白检查通过。Windows 不能编译 GroundGrid 的 ROS/grid_map 集成部分，仍须以 Ubuntu Release 构建和连续完整巡回判定结果。

### 最新 Ubuntu 回传与当前修复：全路线执行连续性及地面回波盲区

`b91ba2ca6761d8d4d3a111c7f461475c212fde5d` 的完整 `mixed / n_trials=3 /
debug_control=true` 回传中，普通闭环通过，多目标统计因巡回进入原目标半径率
`11/12` 失败。总任务完成 13/16；第一个和第三个可解困难目标以安全吸附完成，
第二个困难目标超时，预设无解目标中止。碰撞 0、输出一致性 1.0、陈旧跟踪 0、
条件恢复率 1.0；这些是该次运行结果，不构成 99% 或五场景正式基线。

- 唯一未完成巡回从 `(-7.311,-6.098,0.083)` 驶向 `(-7,-9,-pi/2)`，最终到达
  `(-7.169,-10.081,-2.213)`。完整日志显示规划器约每 0.5 s 发布新路线，并在
  y≈-10 m 交替选择 Forward/Reverse 起步；跟踪器还未完成当前复合动作就被新轨迹
  重置。欧氏目标距离在必要绕行时暂时增加，又误触发两次无进展恢复。当前改为：
  每张新地图仍检查当前车体、连接段和全部未执行扫掠；只要完整目标路线仍安全且
  剩余角点运动量持续下降，就重发同一路线而不重新搜索。路线失效或真正无进展仍
  立即停车/恢复，没有把 0.5 m 到达门槛放宽为 0.6 m。
- 第二困难目标失败处的空格距雷达约 1.3–2.5 m，超出 1.2 m 自车排除圆盘、但仍在
  最低束形成的约 2.16 m 地面回波孔内。格子感知坡度为 16–21°，共享解析 `mixed`
  地形同位置约 1–2°，且无当前有效点、置信度低；附近 `(0,-2)` 岩石的插值污染仍
  可进入该环带。当前新增独立 `ground_support_radius=2.5 m`，只给没有当前有效回波
  且没有历史直接高程的格子使用采集时刻车体支撑平面。历史直接高度和当前有效
  回波始终优先，圆盘外估计不变；下游同为 2.5 m 的盲区已知例外由此获得对应地形
  支撑，而不是绕过已知危险。

本批新增纯函数回归，覆盖绕行/原地转动的剩余运动量、完整路线复用安全门，以及
扩大地面支撑圆盘后历史/当前证据优先和圆盘外不变。Windows 上以 C++17、
`-Wall -Wextra -Werror` 重新编译，36 项滑移/控制、53 项跟踪/停车、45 项规划安全、
53 项回退恢复及 14 项盲区支撑共 201 项检查通过（复核修正此前多计的 1 项）；33 项纯 Python 测试、19 份 Python
语法、10 份 XML、六份 Claude 归档 SHA-256 和差异空白检查通过。尚未在 Ubuntu
编译或完成闭环，不能先宣称巡回和困难目标已修复；原始诊断日志由用户复制为本地
未跟踪文件 `full-mixed-n3.log`，不得提交到源码分支。

### Ubuntu 回传：`ce0908f` 可解任务通过，无解目标未退出

用户按完整 `mixed / n_trials=3 / debug_control=true` 流程回传 `TEST_RC=2`：
`test_closed_loop` passed，统计测试在预设无解目标 `(3,0)` 缺少 `aborted` 时失败。
随后收到本轮完整 JSON 与终端日志，确认 12/12 巡回及三个可解困难目标全部完成，
15/15 可解目标均产生名义路线；无解目标未到达、未产生名义路线，但 35 s 超时。
16 项均有真值间隙观测，碰撞 0，最小净空 0.4657 m；原子输出一致性、角速度覆盖率
均为 1.0，陈旧停车为 0。规划耗时均值 58.94 ms、最大 1001.0 ms，最大值仍超出
任务书严格小于 1 s 的目标；规划器 CPU 均值 10.94%、最大 101.8%、超预算采样比例
0.00922，不代表感知与规划整体资源达标。后续断言未执行完毕，整轮仍未通过。

无解目标 `goal_id=17` 在 `1788614999.5227`、`5008.5236`、`5017.5224`、`5026.0235`
先后启动四次 BackOut，每次均在实际余量恢复后返回 Relax。运动来自原地转动历史，
XY 始终为 `(6.2488,-7.0657)`，故 JSON 行驶距离 0 不代表跟踪器没有执行；1 m
预算包含角点转动。四次回退完成及准备停车的精确确认均正常收到；随后目标仍无解，
最终 `escalation=0/action=relax/fails=18`。这确认是 `finishBackout(true, ...)`
重开整个恢复阶梯造成循环，而不是缺少停车确认或单次搜索耗尽预算。
恢复总进入 4、确认 3、中止 0，原公式给出条件恢复率 0.75，亦低于后续 0.8 门槛；
修复退出后必须重新采集实际 Abort 计数，不能修改统计文件来推断通过。

本次原始 rostest 日志为 `~/p0-results-ce0908f/roslog/rostest-bjm-3212170.log`；
`test_results` 汇总为 3 tests、0 errors、1 failure，其中流水线类包含 2 项测试。
该汇总不包含单独执行的五组纯 C++ 自检；本轮 BUILD_RC、SELF_RC 尚未单独回传。

### 当前修复：回退后的目标重试有界退出

- 成功 BackOut 完成后仍先停车并取得精确确认与更新后的 TF，再开放一次原
  `recovery_step_timeout=2.0 s` 的 Relax 窗口。原 `recovery_confirm_count=2` 保持不变；
  未能确认目标路线就直接进入 Abort，不再循环 Rotate/BackOut。失败回退仍按原流程
  停车后 Abort，新任务及成功退出恢复会清理窗口状态。
- 无 ROS `PostBackoutReplanWindow` 冻结首次实际重试时刻的截止时间；失败、发布和
  重复请求不续期，时钟回退/非法时间失败关闭。等待停车确认不占用窗口；没有确认
  就保持原停车门控。恢复成功/中止计数仍由原目标状态机提交，未修改统计或35秒试验期限。
- 新增回归使用本轮首次回退后的时间序列，确认重试在旧第二次回退之前耗尽；覆盖
  停车/新 TF 门控、重复请求、时钟异常、有效目标仍可连续确认及新恢复周期清理。
  这不代替 ROS 编译和完整连续巡回，尚不能把候选修复记为端到端通过。

本轮 Windows 验证：C++17、`-Wall -Wextra -Werror` 重新编译运行 66 项回退恢复
（新增 13 项）、36 项滑移/控制、53 项跟踪/停车和 45 项规划安全检查，共 200 项
全部通过。19 份 Python 语法、10 份 XML 解析、六份 Claude 归档 SHA-256 与
`git diff --check` 通过；Ubuntu ROS 节点构建及本轮完整巡回仍待回传。

### 最新 Ubuntu 回传：`72df499` 无解目标退出正常，第三困难目标耗时超限

本地收到 `72df499-results/commit.txt`、完整终端日志及指标 JSON，commit 为
`72df49918def83c34581156584a4c5b714fb4fba`，场景为完整 `mixed / n_trials=3`。
普通闭环通过；统计测试在第三可解困难目标 `(8,-5)` 超时时失败，`TEST_RC=2`、
`RESULTS_RC=1`。12/12 巡回和两个可解困难目标完成，即可解任务完成 14/15；
15/15 可解任务均产生名义路线，不能将该规划成功率当作任务完成率。
16 项均有间隙测量，碰撞 0、最小真值车体间隙 0.4932 m；输出一致性和角速度覆盖率
均 1.0、陈旧停车率 0。恢复进入 3、确认 2、中止 1，条件恢复率 1.0；它不抵消未完成任务。
规划耗时诊断样本均值 279.11 ms、最大 1001.0 ms，最大值仍超出任务书严格小于 1 s。
该数组会重复收录最近一次耗时，不可将其 58 个样本或重复的 `1001.0` 当作实际搜索次数。
规划器 CPU 均值 11.48%、最大 101.8%，超预算采样比例 0.03077，非全链路资源验收。

- 无解目标 `goal_id=17`：一次 BackOut 后于 `1788684405.6072` 收到精确停车确认，
  `1788684406.1065` 开始最终 Relax 窗口，`1788684408.6065` 耗尽并 Abort；
  试验共 14.0237 s，未到达且没有名义路线，旧循环未复现。这一项在源码断言顺序上
  被前一失败挡住，但上述终态由独立 JSON 与日志确认，不是从总测试结果推断。
- 超时目标 `goal_id=16`：发出于 `1788684359.1852`，起点 `(-0.8382,-0.1493,0.4787)`。
  原先吸附候选 `(9.575,-3.375,-0.785)` 的三次已记录搜索耗尽约 1 s 单次预算；
  恢复 Rotate 被感知危险格拒绝，随后执行一次安全 BackOut。退后重新搜索时候选
  改为 `(6.275,-6.525,0.785)`；第一条目标路线发布约 12.96 s 后，确认恢复约 13.51 s 后。
  `snapGoal` 当前仅对第一个含合法车体姿态的方形环按目标偏移、航向偏移和地形代价
  选一个候选，再单目标搜索；评分不考虑从当前车位到候选的路径可达性或行驶代价。
  这解释了需要进一步检查候选策略，但日志没有完整地图，不能声称早期候选必然不可达。
- 已接受轨迹的本地跟踪编号 `id=23`（不是 `goal_id`）有 73 个点，长 14.5286 m，
  倒车段 8.3371 m；倒车限速仍为 0.695 m/s。路线确认后持续前进，没有撤销该路线或
  跟踪失效记录；到 35 秒时位置为 `(5.2889,-6.3557,-0.8032)`，距执行终点约 1 m，
  状态仍为 `success_snapped`，没有双方完成信号。所需行驶加多次停转不能由“曾产出路线”替代。

为区分跟踪死锁与执行时间不足，本地诊断 `72df499-results/replay_trajectory.cpp`
读取日志中完整的 73 点，用生产 `TrajectoryTracking`、默认有效运动模型、0.05 s 步长
和首条跟踪记录的舍入位姿复现；每 0.5 s 重发同一路线。C++17、`-Wall -Wextra -Werror`
编译运行成功。在原截止点附近离线位置 `(5.3263,-6.3887,-0.8600)` 与实测接近；
固定路线约 29.70 s 完成，若叠加本轮首次跟踪前等待约为 42.71 s，执行终点误差
0.2278 m、航向误差 0.1736 rad。该复现未包含地图变化、ROS 消息、停车门控或碰撞校验，
不是扩大真实试验时间后的通过记录，不据此调整 35 s 门槛。

本轮没有修改运行源码或测试条件。上述数据和诊断程序留在本地结果目录，不提交原始日志；
项目状态文档记录结论。下一步建议先取得可重放的失败地图/起终点，评估吸附候选选择与
搜索效率，并在原单次预算及完整安全检查下验证；涉及原 P0 排除的搜索策略，待用户确认。
Ubuntu 原始日志为 `~/p0-results-72df499/roslog/rostest-bjm-3796893.log`；
XML 汇总 3 tests、0 errors、1 failure，原始 XML、构建及自检日志尚未复制到本地。

### P0 实现完成、待 Ubuntu 验证：滑移辨识

月面解析地形已提取为仿真器和辨识器共用的无 ROS 模块，最小二乘核心也已拆为纯 NumPy。辨识器使用场景实例、时间戳和锁丢弃命令切换后的 settle-window 样本；NaN、无效区间、激励不足和退化矩阵不会进入或伪装成成功求解，失败时不写参数文件。合成数据测试最大误差门槛为 0.02；仍需用 `IdentifySkidSteer.launch` 验证注入参数恢复。

### P1：测试门槛与任务书不完全一致

- 只有 `mixed` 和 `flat` 属于严格成功率场景。
- `plan_success_all` 当前断言阈值为 90%，任务书要求规划成功率至少 99%。
- CPU 指标只覆盖规划器进程，任务书要求的是目标平台上感知与规划整体占用低于 40%。
- 默认 `n_trials=3` 只适合冒烟检查；正式数据至少使用 10 次重复试验。
- JSON 允许写出 Python 的 `NaN`，它不是严格标准 JSON。

### P1：剩余构建与文档债务

- 当前 RViz 配置主要显示感知图层，规划路径、吸附目标和诊断仍需手动添加。
- `GroundGridNodelet.cpp` 的旧图像发布路径包含硬编码索引 `data(181,181)`，不应视为通用地图中心。

## 5. 尚未完成的任务书能力

- 三维占据栅格或等价的完整三维环境表达。
- 公里级兴趣点/航点任务规划，以及全局与局部规划衔接。
- 视野外与历史障碍物的持久化记忆。
- 地图分辨率、范围和存储形式对精度与实时性的系统实验。
- 相机、TOF 与 LiDAR 的融合感知，以及任务书中的识别、尺寸、位置和坡度精度指标。
- 模型压缩、NPU 推理、Atlas 200i C++ 部署与全系统资源测试。
- 真实月面类环境、真实车辆或外部数据集验证。

## 6. 验证记录

2026-08-28 在 Windows 接手审计中完成：

- 12 个 Python 文件通过纯语法编译检查。
- 10 个 package/launch/rostest XML 文件通过解析。
- 纯 C++ `SkidSteerModel`/`MotionPrimitiveLibrary` 成功使用 GCC 13.2 编译。
- `skidsteer_selfcheck` 的动力学 5 项和轨迹控制 11 项检查全部通过；控制项包含倒车
  几何反馈方向、前视原地旋转/终点保护、角速度前馈混合、限幅与非有限输入。
- 运动基元生成器可编译，默认生成结果与跟踪文件无内容差异。
- P0 分支的 5 项纯 NumPy 辨识测试覆盖已知五参数恢复、settle/NaN 样本、退化激励、解析地形和提取前后数组结果一致性。
- 合成五参数恢复最大绝对误差为 0.00344，加入 settle/NaN 样本后结果最大变化为 0。

Windows 环境没有 ROS1 Noetic，因此以上结果不是 ROS 闭环通过证明。

2026-09-03 恢复缓存交接修复的 Windows 检查：两组自检以 C++17、`-Wall -Wextra -Werror` 编译，`skidsteer_selfcheck` 33 项、`trajectory_tracking_selfcheck` 37 项通过（后者新增 10 项缓存/跟踪联动检查，包含旧缓存首段旋转拒绝复现、确认恢复替换、停止撤销复用、机动不污染历史及新目标清理）。7 项任务完成计数测试、5 项纯 NumPy 辨识测试、16 份 Python 语法检查、10 份 XML 解析通过；六份 Claude 归档 SHA-256 均与清单一致。ROS 构建与完整巡回仍待 Ubuntu 回传。

2026-09-03 针对运动段跟踪修复的 Windows 检查：

- GCC C++17、`-Wall -Wextra -Werror` 编译两个自检程序；原 `skidsteer_selfcheck` 33 项、新 `trajectory_tracking_selfcheck` 27 项全部通过。
- 新自检包含旧跨倒车边界错误复现，正/倒车直线与弧线、旋转/倒车/前进复合轨迹、重复发布、跨角度环绕的长旋转、位置捕获、限幅和非有限输入。
- 5 项纯 NumPy 辨识测试通过；所有受版本控制 Python 源码完成纯语法检查，package/launch/rostest XML 完成解析。
- 此批没有调整 YAML 参数数值，仅改注释；Windows 当前 Python 未安装 PyYAML，未宣称完成 YAML 解析。

用户回传 `80e5d56` 短直线诊断：`DIAG_RC=0`，最后规划器记录执行终点位置误差 0.190 m、航向误差 0.174 rad、请求目标误差 0.135 m。日志在 Ubuntu 的 `~/p0-results-80e5d56/straight.log`，原始 rostest 日志为 `~/.ros/log/rostest-bjm-1166846.log`；这里只收到摘录，未形成完整正式归档。

用户后续回传 `878cbdfd6e0651bd4dd0ea6619fe8197dcbc5b31` 的三项 `mixed` 短诊断（每项一次）：

| 诊断 | 返回码 | 执行终点位置误差 | 执行终点航向误差 | 请求目标位置误差 | Ubuntu 日志 |
| --- | --- | --- | --- | --- | --- |
| 直线 | `DIAG_RC=0` | 0.239 m | 0.173 rad | 0.184 m | `~/p0-results-878cbdf/straight.log`；`rostest-bjm-1189636.log` |
| 偏置转弯 | `TURN_RC=0` | 0.176 m | 0.164 rad | 0.230 m | `~/p0-results-878cbdf/turn.log`；`rostest-bjm-1194550.log` |
| 近障困难目标 | `HARD_RC=0` | 0.248 m | 0.113 rad | 1.005 m（安全吸附） | `~/p0-results-878cbdf/hard-goal.log`；`rostest-bjm-1196997.log` |

以上数值均来自规划器 `execution endpoint reached ... retiring goal` 日志，rostest 原始日志位于 Ubuntu 的 `~/.ros/log/`。困难目标原始位置不安全，测试允许安全吸附，因此必须使用实际执行终点误差判断控制完成，不能把 1.005 m 当作普通目标到达精度。途中旧轨迹扫掠段 `53 -> 54` 被新地图判为无效后，规划器发布新轨迹 `id=2` 并完成任务；提供的摘录未出现 `START footprint invalid` 或 `follow_phase_invalid` 警告。

三次 `test_closed_loop` 均通过了双方完成状态、无碰撞断言和无陈旧停车断言，但没有回传完整安全距离统计与 XML；`test_metrics_over_trials` 在这些短诊断模式下被跳过。这三次只能记录“三项单次短闭环通过”，不能外推完整巡回、99% 成功率或五场景验收。

后续 `878cbdf` 完整 `mixed / n_trials=3` 的用户回传记录：

- `TEST_RC=0`；`catkin_test_results` 报告 8 tests、0 errors、0 failures、0 skipped，耗时 3 分 42.7 秒。
- 16 项目标 = 12 项巡回 + 4 项困难目标；碰撞 0，16 项均有安全距离测量，轨迹输出一致性 1.0，角速度曲线覆盖率 1.0，陈旧停车率 0。
- 巡回规划成功率与进入目标距离阈值率均为 1.0，但按回传双方完成信号重新统计，只有 **11/12（91.67%）** 完成；全部尝试中 11/16 完成（包含故意无解目标，不能直接用作可解任务成功率）。
- 第 10 项 `(-7,-9)` 行驶 8.708 m，最终 `(-7.3114,-8.7278,-0.4891)`；距请求目标约 0.414 m，但与目标航向 `-pi/2` 相差约 62°。规划器与跟踪器完成信号均为 False，状态含 `recovery_relax,success`；尚未完成，不应被判作成功任务。
- 第 13 项 `(-1.2,-2)` 与第 15 项 `(8,-5)` 均曾 `success_snapped`，随后 `aborted`，双方完成信号均为 False。各自最小真值车体间隙为 0.379 m、0.131 m，未发生碰撞，但近障任务仍未完成。第 14 项无名义计划；第 16 项是预设无解目标。
- 含困难目标规划成功率为 14/15（93.33%），仍低于任务书 99%；该指标只证明曾产出名义路径，不能替代执行完成。CPU 超标采样率为 0.005747，非全系统 CPU 占用率。
- Ubuntu 文件：`~/p0-results-878cbdf/full-mixed-n3.log`、`~/p0-results-878cbdf/catkin-test-results.txt`、`~/.ros/planner_metrics_mixed.json`、`~/lunar_ws/build/groundgrid/test_results/`；原始 rostest 日志 `~/.ros/log/rostest-bjm-1204001.log`。当前本地只收到终端摘录及三项明细，原始 JSON/XML/完整日志仍需归档。

`ca66499c6939d2fea2bbcf1b092c7f399f573523` 后续 Ubuntu 完整 `mixed / n_trials=3`：

- `test_closed_loop` passed，`test_metrics_over_trials` 因近障恢复率 0.6667 < 0.8 失败，`TEST_RC=2`；原始日志 `rostest-bjm-1243910.log`。
- 回传 12/12 巡回完成、16 项真值间隙测量、碰撞 0、输出一致性 1.0、陈旧停车率 0。第 8 项经 Relax 恢复完成；第 13、15 项产生过吸附路径但最终中止，第 14 项 13 ms 即退出、第 16 项属于预设无解目标。
- 汇总恢复进入 4、成功 2、中止 1，产生 `2/(4-1)`；该数值受上述终态快照缺口影响，不可据此反推真实恢复率或把修正后的假设值视为通过。
- 文件位于 `~/p0-results-ca66499/full-mixed-n3.log` 与 `planner_metrics_mixed.json`。本地收到明细和控制摘录，完整 ROS 日志/XML 尚未归档。此前所有跨目标指标应以带目标标识的新测试复核。

## 7. 推荐执行顺序

1. 已完成：首包旧策略采集、真实 grid_map 对照，以及 fabd8a0 对全部 39 份输入的 Ubuntu 重放，不重新采集或重复重放。
2. 已审核旧/新策略的安全性、路线/倒车长度、固定扩展预算及真实计时。允许显式指定 reachable_cost 进入闭环；仍不放宽35秒门槛，离线通过不代表任务完成。
3. 五参数注入辨识及新策略三轮独立 arcs / mixed / n_trials=3 已通过。旧策略包测试失败仍保留，完整包测试全绿要求没有取消。仅在需要排查缓存时清理 groundgrid 包缓存。
4. 先保持df032de代码完成一次 dynamics / mixed / n_trials=3（保存快照和控制日志），两种基元审核后统一决定默认启用及复核包测试；再做弧线模式五场景各 n_trials=10。保留所有失败结果，不能挑选成功轮次。
5. 归档 JSON、rostest XML、完整日志、辨识参数、配置、快照和 commit SHA；验证完成前不合并 main。
6. 如出现回归，在同一 P0 分支修复并复跑；不得降低 99% 任务书门槛掩盖缺口。
7. 在基线审核后进入公里级任务分解、航点与局部规划衔接和分块历史地图；传感器融合、Atlas/NPU 与实地验证另立阶段。

## 8. 关键资料

- `任务书.pdf`：合同/验收要求。
- `技术要点清单（总）(改)(6).docx`：16 项技术路线与 11 项软件设计要求。
- `[21]_GroundGrid_LiDAR_Terrain_2405.15664.pdf`：上游 GroundGrid 论文；论文性能不能当作本月球车分支性能。
- `docs/project-memory/claude/`：Claude 阶段历史记录；使用前必须与当前代码和新测试交叉核对。
