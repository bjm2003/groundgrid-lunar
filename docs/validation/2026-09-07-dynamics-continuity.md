# 动力学连续基元接缝与跟踪进度

## 本轮Ubuntu结果：仍未通过

- 归档：p0-smoke-dynamics-f24085f-20260906-232527.tar.gz，100650199字节。
  SHA-256：d441c8ec9f8be2cd67f5a986b58f995692c647054e90207bebf8b8b663d260d2。
- 提交：f24085fe5d779ac9860b0c9a0d557d11002cbc40；跟踪工作区干净，实际参数为
  reachable_cost / dynamics / mixed / n_trials=3 / repeat=1，无模式回退和Conda。
  189份清单文件全部散列匹配，源输入与提交一致；跳过1个软链接。
  51份规划快照全部写出，没有丢失或写盘失败。
- 八项C++预检通过。普通闭环通过，完整巡回测试失败；ROS=2、XML=1、SUITE_RC=1。
  唯一archive_errors来自失败断言XML，不是归档损坏。
- 三个可解困难目标均完成安全吸附终点，(8,-5) 本轮20.823秒完成，不再零位移
  超时；无解(3,0)在13.014秒Abort。但巡回第2、4、12项均25秒超时，完成9/12、
  进入请求半径11/12。到过半径不等于完成姿态收敛和任务停车。
- 碰撞0、陈旧0、原子合规率1.0。49次唯一且目标关联的完整规划尝试：均值
  403.980ms、P95=1003.060ms、最大1003.453ms，不能宣称规划<1秒达标。

## 故障证据

第2项goal_id=3，首次attempt=3从
(-7.237892390,-6.019802486,0.238001694)规划到(-7,-9,-π/2)。
原基元积分末端不是格心；下一段却从格心/朝向bin中心重新出发。在平移接旋转
处，导出相邻点出现几厘米位移，而对应v=0、w非零。跟踪器按几何识别出
reverse阶段，却找不到合法平移命令，报告missing_phase_command（54→55）。

实时日志中车辆位于(-6.056,-7.859,-1.814)，该阶段端点距离0.204m。
ROS适配层错误时调用tracker.clear()；相同保留路线重发便忘掉已完成阶段，
重启距车2.133m的最初旋转/平移。这解释了目标附近停车报错后又走回旧路线，
不是纯粹的搜索超时或正常运动速度不足。

第4及12项分别对应初始attempt=9、24。将三条原始记录路线送入生产无ROS跟踪
核心，在4.70/4.75/4.95秒分别复现同类接缝报错。首例位姿
(-6.05474,-7.86264,-1.82819)与实时首例相符。离线固定路线不包含后续地图更新、
TF消息输送、恢复状态机或实车动力学，不能用它认证闭环。

## 实现边界

1. 动力学搜索节点保存实际积分位姿，栅格/朝向键只用于索引和状态去重。
   下一个基元从真实末端积分，重建使用同一父位姿；不再制造回格心的物理跳变。
   父节点扩展后不再覆盖，连续位姿缓存仅在一次规划调用内存在。
   根部仍保留原起点量化及实际起点连接检查。
2. 实际连续终点直接检查原吸附距离、朝向范围和矩形足迹，不借用同格另一姿态
   的安全证书。输出selected_goal与导出最后一点一致，并重算实际偏移。
3. 非目标集合搜索时，精确目标格/bin若处于基元中途，可使用已有积分采样前缀；
   不插值造点，不扩大目标容差。平移至少保留两个采样，避免停车包络后全零。
   当前等时、恒命令基元的原代价按采样比例分摊。完整基元仍作为另一个后继，
   不能因截短端点不安全删掉完整通过路线；共用原搜索队列及预算。
4. 仍检查完整矩形扫掠、未知规则、原较大吸附间距及f24085f首导出采样起步门槛。
   被移除的是不再发生的格心接缝检查，不是对任何实际运动跳过安全审核。
5. 跟踪阶段报错清空可执行路径并输出零命令，但保留该几何的进度。相同路线
   重发不能从已完成阶段重新开始；新目标、空/非法替换仍撤销，真正不同路线重置。

没有修改基元生成器或已跟踪基元文件、速度/恢复参数、安全间距、吸附半径、
ROS消息/话题、测试时限或断言阈值。默认策略仍legacy_nearest。
连续位姿加离散键去重仍为受限搜索，不声明完备性或全局最优路线。

## Windows验证

- 七个纯C++自检以GCC13.2、C++17、O2、-Wall -Wextra -Werror编译运行通过。
  新增生产核心的平移→旋转连续接缝、精确目标前缀、实际端点范围/朝向审核，
  以及生产跟踪核心的相同故障路线重发不重启、真正新路线恢复执行回归。
- 50项Python/NumPy/观测/归档测试、22份Python语法、10份XML解析通过；
  六份Claude原始归档SHA-256不变，git diff --check通过。
- 同输入固定500次扩展对照共395次：df032de三轮弧线121份×新旧2次，
  f24085f动力学51份×旧1次/新2次。121份弧线逐值不变（计时除外）；动力学
  18份失败保持不变，33份成功仍成功，全部导出通过生产安全审核，重复确定一致。
  另外核对实际所选终点等于路径末点、吸附范围不扩大，以及收到的零ICR基元
  的纯旋转点不包含虚假平移。固定扩展实验不是实时性能认证。
- 三个初始输入重新规划后，生产跟踪核心分别7.10/9.95/8.90秒完成固定路线。
  这只说明已复现的结构性接缝错误消除，不证明更新地图中的任务必然完成。
- 本地对照产物：build/continuous-dynamics-replay.json，SHA-256
  5e57aa56414aabdcca4bc2d7db800bd7d8f5840bfe1b456106e87b01ab75b594；
  build/frozen-tracking-comparison.json，SHA-256
  4e8f8691137a6231ef2acbe4b7dc68ac9a6ea5d77f218b03810305895c651f2c。
  原始输入和输出保留在接收归档及本地build目录，不提交日志/构建产物。

## 下一关

更新分支、Ubuntu Release构建后，重新运行一次完整reachable_cost / dynamics /
mixed / n_trials=3，保留全部JSON、XML、快照、参数和真实返回码。不重复辨识。
修复版ROS结果仍待用户回传；旧弧线三轮通过不能自动认证本批代码。
随后复核完整包测试零失败及五场景n_trials=10，验证完成前不合并main。
目标平台CPU、5km/h、实地/实车验证和公里级系统仍是独立验收缺口。
