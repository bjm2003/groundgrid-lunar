# 2026-09-06 新策略三轮弧线闭环及五参数辨识通过

## 归档与运行身份

- 运行提交：df032de3c7b1c01c12c5fc4d03cd897601426fca，跟踪工作区干净。
- 参数：mixed、n_trials=3、repeat=3、reachable_cost、arcs，
  开启 debug_control 和 planning snapshots，先执行辨识。
- 包：p0-smoke-arcs-df032de-20260906-213446.tar.gz，238058208 字节。
  SHA-256：91f395be40669390be07eda594f6f163ae6f386a4d6548dd146243a6974b5e4a。
- 434 份清单文件的 SHA-256 全部匹配，归档源配置与上述提交逐字节一致。
  解包到 build/received-p0-smoke-arcs-df032de-20260906-213446，跳过4个软链接。
  首次读取时文件仍在传输，尾部不完整；本报告只使用传输完成、全量校验后的包。
- 八项 C++ 预检的日志及返回码全部通过，包含实际 grid_map 索引对照。
- 三轮各自启动独立 ROS 进程，使用不同 run_id 和日志/结果目录；指标、参数、
  planning_attempt 和完整性检查的实际模式均为 reachable_cost / arcs。
  没有以请求参数代替实际运行模式，也没有删去失败轮次。

## 五参数辨识

辨识使用独立 ROS master，输出只写入结果目录，没有覆盖源配置。
identify.log、输出 YAML、result.json 均已收到，退出码为0。

| 参数 | 注入值 | 恢复值 | 绝对误差 |
| --- | --- | --- | --- |
| alpha_v | 0.90 | 0.899804 | 0.000196 |
| alpha_w | 0.85 | 0.850000 | 0.000000 |
| x_icr | 0.15 | 0.152391 | 0.002391 |
| slope_slip_gain | 0.30 | 0.288129 | 0.011871 |
| slope_grade_gain | 0.40 | 0.395783 | 0.004217 |

重新按注入值计算的最大误差为0.011871，满足本阶段 ≤0.02 的辨识门槛。
这验证仿真辨识流程，不是实车土壤参数标定或真实平台滑移补偿验收。

## 三轮完整闭环

各轮 pipeline XML 均为2项测试、0 errors、0 failures，rostest启动器各1项也通过。
ROS/XML 返回码均为0，归档核验错误均为空。

| 指标 | 第1轮 | 第2轮 | 第3轮 |
| --- | --- | --- | --- |
| 巡回进入原请求半径／任务完成 | 12/12，12/12 | 12/12，12/12 | 12/12，12/12 |
| 可解困难目标完成安全吸附终点 | 3/3 | 3/3 | 3/3 |
| 无解目标 (3,0) | Abort | Abort | Abort |
| 碰撞／陈旧跟踪 | 0／0 | 0／0 | 0／0 |
| 原子输出合规率 | 1.0 | 1.0 | 1.0 |
| 第三困难目标 (8,-5) 完成耗时（s） | 14.412 | 14.424 | 15.022 |
| 独立进程运行总时长（s，含启动/退出） | 209.8 | 230.7 | 211.8 |
| 唯一规划尝试数（指标任务，不含预检目标） | 37 | 41 | 40 |
| 完整尝试均值／P95／最大值（ms） | 30.172／125.386／140.954 | 40.772／125.787／127.418 | 34.257／125.604／142.577 |
| 快照 submitted=written | 38 | 42 | 41 |
| 快照 dropped／failed | 0／0 | 0／0 | 0／0 |

全部121份快照包含另外3次普通闭环预检目标，其完整尝试最大耗时209.746 ms。
该批实测尝试均低于1秒；不由此推断其他场景或目标硬件必然满足同一门槛。

合计36次巡回进入原请求半径且完成，9次困难目标完成安全吸附终点，3次故意无解
任务正确 Abort。全体 completion_rate=15/16=0.9375 的分母包含故意无解任务，
不应误解为可解任务失败率。全体 reach_rate=0.75，因为困难目标并未进入原请求
0.5m半径。困难目标最终原坐标误差约1.404–3.156m，必须保留其吸附语义。
全部尝试的最大选中吸附偏移为2.988729m，未扩大既有 Relax 的3.0m上限；
车辆的终点捕获容差会使最终原坐标误差略大于该选中偏移。

角速度曲线覆盖率分别为1.0、1.0、0.9375；不把合规率1.0写成每项覆盖率都为1.0。
anomaly分别为0.375、0.5、0.4375，包含恢复、故意无解及其他原有异常分类，
不是碰撞率。目标平台全链 CPU、5km/h自主速度、实地验证仍缺证据。

## 审核决定与剩余工作

新策略三轮 arcs / mixed / n_trials=3 冒烟及五参数辨识这一里程碑通过。
它不是五场景正式基线，也不是长期99%成功率的统计证明。

保持已验证运行代码不变，先显式切到 dynamics 做一次同规格独立回归，并保留
完整控制日志和快照；不用重复辨识或本次三轮弧线测试。两种基元审核后再统一
决定默认策略启用及复核完整包测试，再运行五场景各 n_trials=10。
此前 legacy_nearest 包测试失败仍保留，不能以本次独立rostest覆盖或抹去。
本次交付只更新验证记录，不改默认策略、不调参、不合并 main。
