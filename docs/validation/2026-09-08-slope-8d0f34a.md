# 5×5回归失败与测量坐标配对修正

归档p0-slope-8d0f34a-20260908-210609.tar.gz，SHA256
a25aeeb9da939a734117368fbb7dbf0cf0c9f832a52452f4a9629bf795cb59f8。
303份文件匹配、89份快照无丢失，8项C++预检通过。ROS/XML0，但仅2/12
完成吸附、0/12请求到达，10次Abort；正式基线仍失败。

最终起点(-0.065351,-6.103513,1.035884)记录横坡15.49399°，解析足迹格
中心最大13.72597°。actual起点也被拒绝，不能绕过安全检查。扩大拟合窗口
没有解决误差，不保留为已验证改进。

源代码显示elevation_raw保存minGroundHeight，即格内最低回波点高度（保留
既有-0.0001m偏移），原拟合却使用格心位置。新增elevation_sample_x/y在同一
cell lock内仅随最低点更新，与raw历史一起保留；地图新区域同时置NaN。
坡度优先由完整3×3九个真实坐标/高度对拟合，采用中心化协方差求解，缺失
或退化时回退已有3×3地面高度拟合；不用运行时5×5外圈。坡度符号沿用原
矩阵坐标约定，原高度、粗糙度、台阶与障碍通道均不改变。

历史归档没有实际回波坐标，无法诚实声称已对它完成新算法全图重放。新增
合成不均匀采样测试可复现格心假设的高估，新拟合恢复真平面；覆盖大世界
坐标、缺失、共线、正负台阶。63条C++、74项Python通过；新增坐标也进入
邻格日志，下一次ROS运行可检查真实配对。Ubuntu编译与闭环仍待验证。
