# 动力学冒烟通过及运行默认策略推广

## 归档与真实模式

- 归档：p0-smoke-dynamics-425205a-20260907-153440.tar.gz，70972027字节。
  SHA-256：aa6985efd6b471183f7006a9cda3409be4008c67181f293618a68393e78391de。
- 运行提交425205acfa9dc418d4f46e0da97acaf8d73642ad；跟踪工作区干净。
  mixed / n_trials=3 / repeat=1 / reachable_cost / dynamics，实际参数和每次
  生产规划尝试均确认动力学模式，没有静默回退。源配置与提交逐字节一致。
- 144份清单文件SHA-256全部匹配，跳过1个便利软链接。36份快照均写出，
  submitted=written=36、dropped=failed=0。提取于
  build/received-p0-smoke-dynamics-425205a-20260907-153440。
- Noetic、Ubuntu Python3.8.10、无活动Conda。父终端仍遗留旧ROS_HOME/日志路径，
  但run.json确认本轮实际子进程的ROS_HOME、ROS_LOG_DIR、ROS_TEST_RESULTS_DIR
  均隔离到新目录；不能将父环境残留误判为本轮使用了旧XML。
- 八项C++预检通过，含实际grid_map对照。普通闭环及完整巡回两个断言测试全过，
  ROS=0、XML=0、archive_errors=0、SUITE_RC=0，约186.6秒。

## 任务与指标

12/12巡回进入原始请求目标半径且完成任务；曾失败的第2、4、12项分别在
7.863、7.413、7.423秒完成。三个可解困难任务完成安全吸附终点，无解任务Abort。
零碰撞、零陈旧跟踪、原子输出合规率1.0、无未确认目标。
完整日志不再出现follow_phase_invalid。

| 困难目标 | 结果 | 任务耗时 | 最终距原请求坐标 |
| --- | --- | ---: | ---: |
| (-1.2,-2) | 吸附终点完成 | 10.444s | 1.659m |
| (0.9,-1.6) | 吸附终点完成 | 8.414s | 2.544m |
| (8,-5) | 吸附终点完成 | 24.824s | 3.271m |
| (3,0) | 无解，Abort | 13.043s | 不记到达 |

困难任务的planned、mission_completed、reached保持区分：三项mission_completed
为true，但三项原请求半径内的reached均为false。不把吸附终点成功写成原点到达。
记录所选终点的最大snap_distance_m为2.970709m，仍在原Relax的3m范围内；原
记录偏移相对请求的量化目标计算，最终车位还有既有终点停车误差，二者不是同一值。
本次没有改变1.5m普通吸附范围、3m Relax范围或到达判定。

35次任务关联的去重完整尝试：均值76.389ms、P95=858.567ms、最大859.943ms。
另核对快照中的全部36次（含普通闭环预检），最大值同为859.943ms。
这是本轮该Ubuntu机器上的实测，不等于目标平台最坏延迟证明。
总体15/16任务完成的分母包含一个应Abort的无解目标，不能把0.9375解释为可解
任务失败；可解目标15/15完成。有限样本也不构成真实99%可靠性统计证明。

## 决定与未完成项

Ubuntu旧输入重放、df032de三轮独立弧线冒烟和425205a动力学一轮冒烟均已获得
完整证据，按既定推广门槛统一将运行默认设为reachable_cost：YAML、launch、
rostest、节点无参数回退、批量入口一致。默认基元仍为arcs。
旧策略legacy_nearest仍可显式选择；纯核心历史默认保持false，使无此字段的v1
快照仍以旧策略解释，已保存策略的快照不受运行默认变化影响。

本批不修改规划/跟踪算法、基元、限速、安全边界、测试时限或验收断言。
新增运行默认一致性与历史回退保留测试，Windows共51项Python测试通过；
22份Python语法、10份XML解析、七个纯C++自检与六份Claude散列检查通过。
新默认的Ubuntu完整包测试仍待执行，不用此前旧策略的失败或本轮独立rostest
替代包测试结论。下一步Release构建、包测试/显式XML汇总、八项CTest并归档，
随后默认弧线五场景各n_trials=10。先前成功轮保留，但不同提交不能无条件互认证。

正式基线仍未建立；目标平台感知加规划CPU<40%、自主速度至少5km/h、实车/实地
验证尚无达标证据，公里级任务分解及持久化分块地图仍在后续阶段。不合并main。
