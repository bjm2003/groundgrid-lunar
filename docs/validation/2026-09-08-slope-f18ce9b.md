# 历史高度保护后的坡地回归

## 证据

- 提交：`f18ce9ba6f82992230eb7ab56af4ed9885a54100`。
- 归档：`p0-slope-f18ce9b-20260908-003728.tar.gz`。
- SHA-256：`a28570a65a4f6180b7154fbe0c34dc6477c39125dbbaab0ac32ece4887b805c6`。
- slope / arcs / reachable_cost / n_trials=1；非正式基线。
- 132份清单文件散列匹配，32份快照写入完整，8项C++预检通过。
- ROS=0、XML=0、archive_errors=0、SUITE_RC=0。

## 实际结果

四项巡回全部Abort，任务完成0/4、原请求到达0/4，仅第一项产生路线。
第一项移动后停在约(3.900181,-8.142379)，其余三项保持该位置。
无报告碰撞、无陈旧输入，原子输出合规率1.0；这些不证明任务可完成。
31次任务关联规划尝试平均4.850ms、P95 9.602ms、最大10.530ms。
快速拒绝不能当作成功规划的性能证据。

非严格场景不强制到达/完成率，且4次恢复事件、4次Abort使历史条件恢复率
分母(events-aborts)为0，恢复率断言跳过。这解释了全失败仍返回0。
随后增加所有场景共用的非零任务完成保护，直接使用双方确认的完成信号，
不改变该历史恢复指标或99%验收阈值。非零完成只防止空洞通过，不是验收标准；
低于99%的边缘场景仍须报告缺口。新增纯Python回归覆盖全Abort、全超时、
空试验集，以及真实ROS测试断言方法对本次全Abort模式的拒绝。

## 已确认与未确认

同地图邻格日志显示被选中的历史实测格ground/raw/corrected一致，说明高度
保护已生效；不能据此声称全部坡度估计正确。仍存在局部坡度拒绝，例如
(4.875,-7.525)记录梯度约(-0.29515,0.13149)，长期被横坡检查拒绝。

最后attempt-32快照失败为start_no_successor，扩展1个节点、根后继0。
离线生产足迹检查显示实际起点裸足迹有效，但这不证明任何带恢复安全间距的
出发动作有效。应继续逐段区分地图估计、起点量化与真实可行性，不能跳过扫掠
检查或放宽坡度/安全间距。解析地形只用于离线诊断，不输入运行规划器。

本轮未建立正式基线，暂不要求重复相同ROS运行；保留本次失败归档。

## 根动作离线对照

对attempt-32保留同一地图和安全检查，以实际起点构造未量化的理想动作，
短直线倒车通过；生产量化起点的八种动作全部失败。但是将实际起点的倒车
终点恢复为格点/朝向量化后，该动作也失败。因此仅替换起点坐标不够，不能
将理想动作通过当作导出轨迹安全证明。运行规划器尚未修改，需要继续审查
连续端点连接或有观测历史的既有撤退，不允许将不安全的格点连接省略。

第二轮离线实验进一步拆分位置/朝向：actual起点含0.25m间距通过，记录横坡
最大14.91577°；理想短倒车终点仅量化位置仍通过，仅量化朝向即失败。原历史
BackOut被拒绝的点(3.897247,-8.125715,-1.400651)已在完整0.25m带内失败，
不能通过放宽撤退规则绕过。尚无证据表明准备与执行的冻结间距实现不一致。

仅在忽略构建目录的实验副本中，将朝向格相位锚定actual起点，根平移动作从
actual位置出发：attempt-32在5000次固定扩展预算内以59次扩展找到5.545m路线，
导出审计通过。32份输入全量实验结果为10份成功（1、2、3、26至32），14份
start_no_successor（4至17），8份goal_invalid（18至25）。失败结果的审计true
只是空输出合规，不代表存在安全路线。该实验还改变终点朝向离散相位，需审查
目标契约、重构起点与精确导出一致性；未作为生产修改、未改变快照格式，不能
宣称解决所有失败，也不能用这份实验替代Ubuntu闭环验证。

## 后续范围确认

首个巡回任务在1788799068.985秒因更新地图上的active_sweep被撤回：
(4.125,-9.625)记录坡度20.532°、cost=100，同一坐标解析坡度17.239°；
(4.875,-7.525)记录17.906°、解析13.952°。这些中心点对照不能替代整个
足迹的真实连续地形认证，但足以说明不能将所有拒绝都归因于朝向量化。
当前梯度来自相邻格高度中心差分；原始点落在格内的位置、平面补值与历史高度
混合均可能影响估计，不能凭中心值唯一确定误差来源。

下一项拟议工作是地形坡度估计的独立修正与台阶/负障碍保真回归；这超出此前
目标吸附/搜索改进范围，等待用户确认后实施。安全阈值、间距、原请求目标、
时限保持不变，解析真值仅作诊断/测试，不传给运行规划器。

用户随后批准该范围，并要求先恢复功能。本批采用固定3×3最小二乘平面梯度：
gx=sum(i*z)/(6*resolution)，gy同理，沿用原矩阵轴符号。不增加窗口或调参，
不修改高度矩阵，原step_height、roughness、obstacle及max-diff计算保持原样。
任一邻格非有限或分辨率无效时返回NaN，不制造零坡度。均匀陡坡保持精确梯度。

同记录邻格计算：(4.125,-9.625)为18.445°，(4.875,-7.525)为14.147°，
(5.325,-8.725)为17.858°；纯平面填补偏差点(1.275,-6.775)仍为15.701°，
不宣称全部误差消除。此处是高度邻格重计算，不是完整地图闭环重放。
38条C++自检覆盖已记录邻格、正反陡坡、正负台阶、不改输入、九处缺失邻格；
74项Python、25份语法/10份XML及六份历史归档散列通过。Ubuntu构建、ROS运行
及其他场景回归仍待验证，先交付一次坡地短回归，不为未验证的完善工作拖延。
