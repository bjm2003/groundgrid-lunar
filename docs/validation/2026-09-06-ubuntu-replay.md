# 2026-09-06 Ubuntu 同输入重放审核

## 来源与完整性

- 运行代码：fabd8a0d11b1fb6ba75be60d12823f2039da99ba。
- 输入来自 7a7294f 的 mixed / n_trials=3 / arcs / legacy_nearest 采集，
  不包含动力学模式或其他场景。
- 回传包：p0-replay-fabd8a0-20260906-205231.tar.gz。
  SHA-256：6bfea791e438737f8f972923f75138f1ce3aed48721fdc8735f97f86a58e6027。
- 353 份清单文件全部通过 SHA-256 校验；39 份输入的散列与本地保存的
  原始采集载荷完全相符，无目标筛选，无重复轮次删选。
- 解包证据位于 build/received-replay-20260906-205231。
- 用户报告 REPLAY_RC=0；归档确认 complete_and_safe=true、errors=[]、
  replay_commit=fabd8a0，使用 Ubuntu Python 3.8.10 和 rosrun 执行生产核心。
  本包不包含完整构建日志及自检日志，不据此补记包测试通过。

## 重放结果

39 输入 × 2 策略 × 2 预算模式 × 3 次重复，共 468 次重放。
固定预算为 5000 次扩展；计时组使用输入中原有的真实时钟预算。

- 468 次均通过导出扫掠和输出合规审核；无路线时仍保留失败记录，
  不能把空输出的审核通过称为成功规划。
- 两策略共 78 个固定预算组，各三次结果和轨迹可重复。
- 旧策略的 234 次输出（含空输出）与原始 Ubuntu 记录逐值相同。
- 两策略均在相同的 21 份输入上找到路线，其余 18 份没有路线。
  其中 13 份安全候选集为空、5 份起点无有效后继。这是单次规划尝试，
  不是 39 个独立任务或闭环成功率。
- 空安全候选集输入的新策略耗时中位数为 0.876–3.295 ms，扩展数为 0；
  已没有此前无候选仍耗满约 1 秒的回归。

| Ubuntu 计时组，全部 117 次/策略 | 旧策略 | 新策略 |
| --- | --- | --- |
| 核心总耗时均值（ms） | 49.151 | 37.884 |
| P95，nearest-rank（ms） | 179.870 | 141.798 |
| 最大耗时（ms） | 315.777 | 183.463 |

均值包含失败尝试；时间包括核心目标处理、搜索和速度曲线生成，但不包括
ROS 输入适配、磁盘读写、离线导出审核或车辆执行。该表不证明目标硬件端到端
时延、全链 CPU、5 km/h 自主速度或 99% 任务成功率达标。

| 快照尝试 | 路线长旧→新（m） | 倒车长旧→新（m） | 三次耗时中位数旧→新（ms） |
| --- | --- | --- | --- |
| 21，第二困难目标 | 6.666→2.696 | 0.924→0.899 | 49.818→21.544 |
| 31，第三困难目标 | 7.989→5.543 | 1.273→0.849 | 43.070→24.977 |
| 32，第三困难目标 | 8.414→4.645 | 1.323→0.424 | 315.111→24.238 |

新旧策略可能选择不同安全吸附终点；这些比较不是同一终点的全局最短路证明。
位置、朝向、地形代价及既有吸附范围仍受生产实现约束；未放宽安全间距、
测试时限或验收阈值。

## 审核决定与下一步

真实快照等价性和 Ubuntu 离线重放关通过，允许显式使用 reachable_cost 进入
新策略闭环回归。默认 legacy_nearest 暂不改变，也不合并 main。

先在 fabd8a0 执行完整包测试并使用全新 XML/日志目录归档，再运行五参数辨识、
三轮独立 arcs mixed/n_trials=3、一次 dynamics 同规格，以及五场景 n_trials=10。
新的 ROS 闭环、辨识和正式基线仍待回传，不能用本次离线结果代替。
